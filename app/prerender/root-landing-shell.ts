// ROOT_LANDING_SHELL_CLASS keeps the static root landing in the same outer
// shell as the prerender startup component before the full app replaces it.
export const ROOT_LANDING_SHELL_CLASS =
  'bg-background flex h-full min-h-0 w-full flex-1 flex-col overflow-hidden'
