// SPACE_BODY_TYPE is the body type id for a space.
export const SPACE_BODY_TYPE = 'space'

// CDN_BODY_TYPE is the body type id recorded on a CDN-backed SharedObject.
// Must match CdnBodyType in core/cdn/sharedobject.
export const CDN_BODY_TYPE = 'cdn.spacewave'

// DEFAULT_OBJECT_LAYOUT_KEY is the default object key for the main layout.
// Used as fallback when SpaceSettings.indexPath is not configured.
export const DEFAULT_OBJECT_LAYOUT_KEY = 'object-layout/main'
