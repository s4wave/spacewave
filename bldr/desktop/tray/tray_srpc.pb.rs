// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.49.7
// source: github.com/s4wave/spacewave/bldr/desktop/tray/tray.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for DesktopTrayResourceService.
pub const DESKTOP_TRAY_RESOURCE_SERVICE_SERVICE_ID: &str = "bldr.desktop.tray.DesktopTrayResourceService";

/// Stream trait for DesktopTrayResourceService.WatchDesktopTray.
#[starpc::async_trait]
pub trait DesktopTrayResourceServiceWatchDesktopTrayStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<WatchDesktopTrayResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for DesktopTrayResourceService.
#[starpc::async_trait]
pub trait DesktopTrayResourceServiceClient: Send + Sync {
    /// RegisterDesktopTrayEntry.
    async fn register_desktop_tray_entry(&self, request: &RegisterDesktopTrayEntryRequest) -> starpc::Result<RegisterDesktopTrayEntryResponse>;
    /// WatchDesktopTray.
    async fn watch_desktop_tray(&self, request: &WatchDesktopTrayRequest) -> starpc::Result<Box<dyn DesktopTrayResourceServiceWatchDesktopTrayStream>>;
    /// InvokeDesktopTrayEntry.
    async fn invoke_desktop_tray_entry(&self, request: &InvokeDesktopTrayEntryRequest) -> starpc::Result<InvokeDesktopTrayEntryResponse>;
}

/// Client implementation for DesktopTrayResourceService.
pub struct DesktopTrayResourceServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> DesktopTrayResourceServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> DesktopTrayResourceServiceClient for DesktopTrayResourceServiceClientImpl<C> {
    async fn register_desktop_tray_entry(&self, request: &RegisterDesktopTrayEntryRequest) -> starpc::Result<RegisterDesktopTrayEntryResponse> {
        self.client.exec_call("bldr.desktop.tray.DesktopTrayResourceService", "RegisterDesktopTrayEntry", request).await
    }
    async fn watch_desktop_tray(&self, request: &WatchDesktopTrayRequest) -> starpc::Result<Box<dyn DesktopTrayResourceServiceWatchDesktopTrayStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("bldr.desktop.tray.DesktopTrayResourceService", "WatchDesktopTray", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(DesktopTrayResourceServiceWatchDesktopTrayStreamImpl { stream }))
    }
    async fn invoke_desktop_tray_entry(&self, request: &InvokeDesktopTrayEntryRequest) -> starpc::Result<InvokeDesktopTrayEntryResponse> {
        self.client.exec_call("bldr.desktop.tray.DesktopTrayResourceService", "InvokeDesktopTrayEntry", request).await
    }
}

struct DesktopTrayResourceServiceWatchDesktopTrayStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl DesktopTrayResourceServiceWatchDesktopTrayStream for DesktopTrayResourceServiceWatchDesktopTrayStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<WatchDesktopTrayResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for DesktopTrayResourceService.
#[starpc::async_trait]
pub trait DesktopTrayResourceServiceServer: Send + Sync {
    /// RegisterDesktopTrayEntry.
    async fn register_desktop_tray_entry(&self, request: RegisterDesktopTrayEntryRequest) -> starpc::Result<RegisterDesktopTrayEntryResponse>;
    /// WatchDesktopTray.
    async fn watch_desktop_tray(&self, request: WatchDesktopTrayRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// InvokeDesktopTrayEntry.
    async fn invoke_desktop_tray_entry(&self, request: InvokeDesktopTrayEntryRequest) -> starpc::Result<InvokeDesktopTrayEntryResponse>;
}

const DESKTOP_TRAY_RESOURCE_SERVICE_METHOD_IDS: &[&str] = &[
    "RegisterDesktopTrayEntry",
    "WatchDesktopTray",
    "InvokeDesktopTrayEntry",
];

/// Handler for DesktopTrayResourceService.
pub struct DesktopTrayResourceServiceHandler<S: DesktopTrayResourceServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: DesktopTrayResourceServiceServer + 'static> DesktopTrayResourceServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: DesktopTrayResourceServiceServer + 'static> starpc::Invoker for DesktopTrayResourceServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "RegisterDesktopTrayEntry" => {
                let request: RegisterDesktopTrayEntryRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.register_desktop_tray_entry(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "WatchDesktopTray" => {
                let request: WatchDesktopTrayRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.watch_desktop_tray(request, stream).await)
            }
            "InvokeDesktopTrayEntry" => {
                let request: InvokeDesktopTrayEntryRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.invoke_desktop_tray_entry(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: DesktopTrayResourceServiceServer + 'static> starpc::Handler for DesktopTrayResourceServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "bldr.desktop.tray.DesktopTrayResourceService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        DESKTOP_TRAY_RESOURCE_SERVICE_METHOD_IDS
    }
}

/// Service ID for DesktopTrayEntryResourceService.
pub const DESKTOP_TRAY_ENTRY_RESOURCE_SERVICE_SERVICE_ID: &str = "bldr.desktop.tray.DesktopTrayEntryResourceService";

/// Client trait for DesktopTrayEntryResourceService.
#[starpc::async_trait]
pub trait DesktopTrayEntryResourceServiceClient: Send + Sync {
    /// SetDesktopTrayEntry.
    async fn set_desktop_tray_entry(&self, request: &SetDesktopTrayEntryRequest) -> starpc::Result<SetDesktopTrayEntryResponse>;
    /// SetDesktopTrayEntryActive.
    async fn set_desktop_tray_entry_active(&self, request: &SetDesktopTrayEntryActiveRequest) -> starpc::Result<SetDesktopTrayEntryActiveResponse>;
    /// SetDesktopTrayEntryEnabled.
    async fn set_desktop_tray_entry_enabled(&self, request: &SetDesktopTrayEntryEnabledRequest) -> starpc::Result<SetDesktopTrayEntryEnabledResponse>;
}

/// Client implementation for DesktopTrayEntryResourceService.
pub struct DesktopTrayEntryResourceServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> DesktopTrayEntryResourceServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> DesktopTrayEntryResourceServiceClient for DesktopTrayEntryResourceServiceClientImpl<C> {
    async fn set_desktop_tray_entry(&self, request: &SetDesktopTrayEntryRequest) -> starpc::Result<SetDesktopTrayEntryResponse> {
        self.client.exec_call("bldr.desktop.tray.DesktopTrayEntryResourceService", "SetDesktopTrayEntry", request).await
    }
    async fn set_desktop_tray_entry_active(&self, request: &SetDesktopTrayEntryActiveRequest) -> starpc::Result<SetDesktopTrayEntryActiveResponse> {
        self.client.exec_call("bldr.desktop.tray.DesktopTrayEntryResourceService", "SetDesktopTrayEntryActive", request).await
    }
    async fn set_desktop_tray_entry_enabled(&self, request: &SetDesktopTrayEntryEnabledRequest) -> starpc::Result<SetDesktopTrayEntryEnabledResponse> {
        self.client.exec_call("bldr.desktop.tray.DesktopTrayEntryResourceService", "SetDesktopTrayEntryEnabled", request).await
    }
}

/// Server trait for DesktopTrayEntryResourceService.
#[starpc::async_trait]
pub trait DesktopTrayEntryResourceServiceServer: Send + Sync {
    /// SetDesktopTrayEntry.
    async fn set_desktop_tray_entry(&self, request: SetDesktopTrayEntryRequest) -> starpc::Result<SetDesktopTrayEntryResponse>;
    /// SetDesktopTrayEntryActive.
    async fn set_desktop_tray_entry_active(&self, request: SetDesktopTrayEntryActiveRequest) -> starpc::Result<SetDesktopTrayEntryActiveResponse>;
    /// SetDesktopTrayEntryEnabled.
    async fn set_desktop_tray_entry_enabled(&self, request: SetDesktopTrayEntryEnabledRequest) -> starpc::Result<SetDesktopTrayEntryEnabledResponse>;
}

const DESKTOP_TRAY_ENTRY_RESOURCE_SERVICE_METHOD_IDS: &[&str] = &[
    "SetDesktopTrayEntry",
    "SetDesktopTrayEntryActive",
    "SetDesktopTrayEntryEnabled",
];

/// Handler for DesktopTrayEntryResourceService.
pub struct DesktopTrayEntryResourceServiceHandler<S: DesktopTrayEntryResourceServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: DesktopTrayEntryResourceServiceServer + 'static> DesktopTrayEntryResourceServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: DesktopTrayEntryResourceServiceServer + 'static> starpc::Invoker for DesktopTrayEntryResourceServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "SetDesktopTrayEntry" => {
                let request: SetDesktopTrayEntryRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.set_desktop_tray_entry(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "SetDesktopTrayEntryActive" => {
                let request: SetDesktopTrayEntryActiveRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.set_desktop_tray_entry_active(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "SetDesktopTrayEntryEnabled" => {
                let request: SetDesktopTrayEntryEnabledRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.set_desktop_tray_entry_enabled(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: DesktopTrayEntryResourceServiceServer + 'static> starpc::Handler for DesktopTrayEntryResourceServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "bldr.desktop.tray.DesktopTrayEntryResourceService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        DESKTOP_TRAY_ENTRY_RESOURCE_SERVICE_METHOD_IDS
    }
}

/// Service ID for DesktopTrayActionHandlerService.
pub const DESKTOP_TRAY_ACTION_HANDLER_SERVICE_SERVICE_ID: &str = "bldr.desktop.tray.DesktopTrayActionHandlerService";

/// Client trait for DesktopTrayActionHandlerService.
#[starpc::async_trait]
pub trait DesktopTrayActionHandlerServiceClient: Send + Sync {
    /// HandleDesktopTrayAction.
    async fn handle_desktop_tray_action(&self, request: &HandleDesktopTrayActionRequest) -> starpc::Result<HandleDesktopTrayActionResponse>;
}

/// Client implementation for DesktopTrayActionHandlerService.
pub struct DesktopTrayActionHandlerServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> DesktopTrayActionHandlerServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> DesktopTrayActionHandlerServiceClient for DesktopTrayActionHandlerServiceClientImpl<C> {
    async fn handle_desktop_tray_action(&self, request: &HandleDesktopTrayActionRequest) -> starpc::Result<HandleDesktopTrayActionResponse> {
        self.client.exec_call("bldr.desktop.tray.DesktopTrayActionHandlerService", "HandleDesktopTrayAction", request).await
    }
}

/// Server trait for DesktopTrayActionHandlerService.
#[starpc::async_trait]
pub trait DesktopTrayActionHandlerServiceServer: Send + Sync {
    /// HandleDesktopTrayAction.
    async fn handle_desktop_tray_action(&self, request: HandleDesktopTrayActionRequest) -> starpc::Result<HandleDesktopTrayActionResponse>;
}

const DESKTOP_TRAY_ACTION_HANDLER_SERVICE_METHOD_IDS: &[&str] = &[
    "HandleDesktopTrayAction",
];

/// Handler for DesktopTrayActionHandlerService.
pub struct DesktopTrayActionHandlerServiceHandler<S: DesktopTrayActionHandlerServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: DesktopTrayActionHandlerServiceServer + 'static> DesktopTrayActionHandlerServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: DesktopTrayActionHandlerServiceServer + 'static> starpc::Invoker for DesktopTrayActionHandlerServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "HandleDesktopTrayAction" => {
                let request: HandleDesktopTrayActionRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.handle_desktop_tray_action(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: DesktopTrayActionHandlerServiceServer + 'static> starpc::Handler for DesktopTrayActionHandlerServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "bldr.desktop.tray.DesktopTrayActionHandlerService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        DESKTOP_TRAY_ACTION_HANDLER_SERVICE_METHOD_IDS
    }
}

