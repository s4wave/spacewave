// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.47.1
// source: github.com/s4wave/spacewave/bldr/manifest/manifest.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for ManifestFetch.
pub const MANIFEST_FETCH_SERVICE_ID: &str = "bldr.manifest.ManifestFetch";

/// Stream trait for ManifestFetch.FetchManifest.
#[starpc::async_trait]
pub trait ManifestFetchFetchManifestStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<FetchManifestResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for ManifestFetch.
#[starpc::async_trait]
pub trait ManifestFetchClient: Send + Sync {
    /// FetchManifest.
    async fn fetch_manifest(&self, request: &FetchManifestRequest) -> starpc::Result<Box<dyn ManifestFetchFetchManifestStream>>;
}

/// Client implementation for ManifestFetch.
pub struct ManifestFetchClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> ManifestFetchClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> ManifestFetchClient for ManifestFetchClientImpl<C> {
    async fn fetch_manifest(&self, request: &FetchManifestRequest) -> starpc::Result<Box<dyn ManifestFetchFetchManifestStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("bldr.manifest.ManifestFetch", "FetchManifest", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(ManifestFetchFetchManifestStreamImpl { stream }))
    }
}

struct ManifestFetchFetchManifestStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl ManifestFetchFetchManifestStream for ManifestFetchFetchManifestStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<FetchManifestResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for ManifestFetch.
#[starpc::async_trait]
pub trait ManifestFetchServer: Send + Sync {
    /// FetchManifest.
    async fn fetch_manifest(&self, request: FetchManifestRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const MANIFEST_FETCH_METHOD_IDS: &[&str] = &[
    "FetchManifest",
];

/// Handler for ManifestFetch.
pub struct ManifestFetchHandler<S: ManifestFetchServer> {
    server: std::sync::Arc<S>,
}

impl<S: ManifestFetchServer + 'static> ManifestFetchHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: ManifestFetchServer + 'static> starpc::Invoker for ManifestFetchHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "FetchManifest" => {
                let request: FetchManifestRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.fetch_manifest(request, stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: ManifestFetchServer + 'static> starpc::Handler for ManifestFetchHandler<S> {
    fn service_id(&self) -> &'static str {
        "bldr.manifest.ManifestFetch"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        MANIFEST_FETCH_METHOD_IDS
    }
}

