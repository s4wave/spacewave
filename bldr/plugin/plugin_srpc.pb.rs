// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.49.3
// source: github.com/s4wave/spacewave/bldr/plugin/plugin.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for PluginHost.
pub const PLUGIN_HOST_SERVICE_ID: &str = "bldr.plugin.PluginHost";

/// Stream trait for PluginHost.ExecController.
#[starpc::async_trait]
pub trait PluginHostExecControllerStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<ExecControllerResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Stream trait for PluginHost.LoadPlugin.
#[starpc::async_trait]
pub trait PluginHostLoadPluginStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<LoadPluginResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Stream trait for PluginHost.PluginRpc.
#[starpc::async_trait]
pub trait PluginHostPluginRpcStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<RpcStreamPacket>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Stream trait for PluginHost.PluginFsRpc.
#[starpc::async_trait]
pub trait PluginHostPluginFsRpcStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<RpcStreamPacket>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for PluginHost.
#[starpc::async_trait]
pub trait PluginHostClient: Send + Sync {
    /// GetPluginInfo.
    async fn get_plugin_info(&self, request: &GetPluginInfoRequest) -> starpc::Result<GetPluginInfoResponse>;
    /// ExecController.
    async fn exec_controller(&self, request: &ExecControllerRequest) -> starpc::Result<Box<dyn PluginHostExecControllerStream>>;
    /// LoadPlugin.
    async fn load_plugin(&self, request: &LoadPluginRequest) -> starpc::Result<Box<dyn PluginHostLoadPluginStream>>;
    /// PluginRpc.
    async fn plugin_rpc(&self) -> starpc::Result<Box<dyn PluginHostPluginRpcStream>>;
    /// PluginFsRpc.
    async fn plugin_fs_rpc(&self) -> starpc::Result<Box<dyn PluginHostPluginFsRpcStream>>;
}

/// Client implementation for PluginHost.
pub struct PluginHostClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> PluginHostClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> PluginHostClient for PluginHostClientImpl<C> {
    async fn get_plugin_info(&self, request: &GetPluginInfoRequest) -> starpc::Result<GetPluginInfoResponse> {
        self.client.exec_call("bldr.plugin.PluginHost", "GetPluginInfo", request).await
    }
    async fn exec_controller(&self, request: &ExecControllerRequest) -> starpc::Result<Box<dyn PluginHostExecControllerStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("bldr.plugin.PluginHost", "ExecController", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(PluginHostExecControllerStreamImpl { stream }))
    }
    async fn load_plugin(&self, request: &LoadPluginRequest) -> starpc::Result<Box<dyn PluginHostLoadPluginStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("bldr.plugin.PluginHost", "LoadPlugin", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(PluginHostLoadPluginStreamImpl { stream }))
    }
    async fn plugin_rpc(&self) -> starpc::Result<Box<dyn PluginHostPluginRpcStream>> {
        let stream = self.client.new_stream("bldr.plugin.PluginHost", "PluginRpc", None).await?;
        Ok(Box::new(PluginHostPluginRpcStreamImpl { stream }))
    }
    async fn plugin_fs_rpc(&self) -> starpc::Result<Box<dyn PluginHostPluginFsRpcStream>> {
        let stream = self.client.new_stream("bldr.plugin.PluginHost", "PluginFsRpc", None).await?;
        Ok(Box::new(PluginHostPluginFsRpcStreamImpl { stream }))
    }
}

struct PluginHostExecControllerStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl PluginHostExecControllerStream for PluginHostExecControllerStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<ExecControllerResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

struct PluginHostLoadPluginStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl PluginHostLoadPluginStream for PluginHostLoadPluginStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<LoadPluginResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

struct PluginHostPluginRpcStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl PluginHostPluginRpcStream for PluginHostPluginRpcStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<RpcStreamPacket> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

struct PluginHostPluginFsRpcStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl PluginHostPluginFsRpcStream for PluginHostPluginFsRpcStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<RpcStreamPacket> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for PluginHost.
#[starpc::async_trait]
pub trait PluginHostServer: Send + Sync {
    /// GetPluginInfo.
    async fn get_plugin_info(&self, request: GetPluginInfoRequest) -> starpc::Result<GetPluginInfoResponse>;
    /// ExecController.
    async fn exec_controller(&self, request: ExecControllerRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// LoadPlugin.
    async fn load_plugin(&self, request: LoadPluginRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// PluginRpc.
    async fn plugin_rpc(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// PluginFsRpc.
    async fn plugin_fs_rpc(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const PLUGIN_HOST_METHOD_IDS: &[&str] = &[
    "GetPluginInfo",
    "ExecController",
    "LoadPlugin",
    "PluginRpc",
    "PluginFsRpc",
];

/// Handler for PluginHost.
pub struct PluginHostHandler<S: PluginHostServer> {
    server: std::sync::Arc<S>,
}

impl<S: PluginHostServer + 'static> PluginHostHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: PluginHostServer + 'static> starpc::Invoker for PluginHostHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "GetPluginInfo" => {
                let request: GetPluginInfoRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.get_plugin_info(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "ExecController" => {
                let request: ExecControllerRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.exec_controller(request, stream).await)
            }
            "LoadPlugin" => {
                let request: LoadPluginRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.load_plugin(request, stream).await)
            }
            "PluginRpc" => {
                (true, self.server.plugin_rpc(stream).await)
            }
            "PluginFsRpc" => {
                (true, self.server.plugin_fs_rpc(stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: PluginHostServer + 'static> starpc::Handler for PluginHostHandler<S> {
    fn service_id(&self) -> &'static str {
        "bldr.plugin.PluginHost"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        PLUGIN_HOST_METHOD_IDS
    }
}

/// Service ID for Plugin.
pub const PLUGIN_SERVICE_ID: &str = "bldr.plugin.Plugin";

/// Stream trait for Plugin.PluginRpc.
#[starpc::async_trait]
pub trait PluginPluginRpcStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<RpcStreamPacket>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for Plugin.
#[starpc::async_trait]
pub trait PluginClient: Send + Sync {
    /// PluginRpc.
    async fn plugin_rpc(&self) -> starpc::Result<Box<dyn PluginPluginRpcStream>>;
}

/// Client implementation for Plugin.
pub struct PluginClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> PluginClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> PluginClient for PluginClientImpl<C> {
    async fn plugin_rpc(&self) -> starpc::Result<Box<dyn PluginPluginRpcStream>> {
        let stream = self.client.new_stream("bldr.plugin.Plugin", "PluginRpc", None).await?;
        Ok(Box::new(PluginPluginRpcStreamImpl { stream }))
    }
}

struct PluginPluginRpcStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl PluginPluginRpcStream for PluginPluginRpcStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<RpcStreamPacket> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for Plugin.
#[starpc::async_trait]
pub trait PluginServer: Send + Sync {
    /// PluginRpc.
    async fn plugin_rpc(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const PLUGIN_METHOD_IDS: &[&str] = &[
    "PluginRpc",
];

/// Handler for Plugin.
pub struct PluginHandler<S: PluginServer> {
    server: std::sync::Arc<S>,
}

impl<S: PluginServer + 'static> PluginHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: PluginServer + 'static> starpc::Invoker for PluginHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "PluginRpc" => {
                (true, self.server.plugin_rpc(stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: PluginServer + 'static> starpc::Handler for PluginHandler<S> {
    fn service_id(&self) -> &'static str {
        "bldr.plugin.Plugin"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        PLUGIN_METHOD_IDS
    }
}

