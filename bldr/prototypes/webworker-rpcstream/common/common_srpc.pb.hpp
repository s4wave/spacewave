//go:build deps_only && cgo

// Code generated by protoc-gen-starpc-cpp. DO NOT EDIT.
// protoc-gen-starpc-cpp version: v0.47.1
// source: github.com/s4wave/spacewave/bldr/prototypes/webworker-rpcstream/common/common.proto

#pragma once

#include <functional>
#include <memory>
#include <string>
#include <utility>

#include "srpc/starpc.hpp"

#include "common.pb.h"

namespace prototype::webworker_rpcstream::common {

// Service ID for PrototypeService
constexpr const char* kSRPCPrototypeServiceServiceID = "prototype.webworker_rpcstream.common.PrototypeService";

class SRPCPrototypeService_PrototypeClient;
class SRPCPrototypeService_PrototypeStream;

// SRPCPrototypeServiceClient is the client API for PrototypeService service.
class SRPCPrototypeServiceClient {
 public:
  virtual ~SRPCPrototypeServiceClient() = default;

  // SRPCClient returns the underlying SRPC client.
  virtual starpc::Client* SRPCClient() = 0;

  // Prototype
  virtual std::pair<std::unique_ptr<SRPCPrototypeService_PrototypeClient>, starpc::Error> Prototype(const prototype::webworker_rpcstream::common::PrototypeRequest& in) = 0;
};

// SRPCPrototypeServiceClientImpl implements SRPCPrototypeServiceClient.
class SRPCPrototypeServiceClientImpl : public SRPCPrototypeServiceClient {
 public:
  explicit SRPCPrototypeServiceClientImpl(starpc::Client* cc, const std::string& service_id = "")
      : cc_(cc), service_id_(service_id.empty() ? kSRPCPrototypeServiceServiceID : service_id) {}

  starpc::Client* SRPCClient() override { return cc_; }

  // Prototype
  virtual std::pair<std::unique_ptr<SRPCPrototypeService_PrototypeClient>, starpc::Error> Prototype(const prototype::webworker_rpcstream::common::PrototypeRequest& in) override;

 private:
  starpc::Client* cc_;
  std::string service_id_;
};

// NewSRPCPrototypeServiceClient creates a new client.
inline std::unique_ptr<SRPCPrototypeServiceClient> NewSRPCPrototypeServiceClient(starpc::Client* cc) {
  return std::make_unique<SRPCPrototypeServiceClientImpl>(cc);
}

// SRPCPrototypeServiceServer is the server API for PrototypeService service.
class SRPCPrototypeServiceServer {
 public:
  virtual ~SRPCPrototypeServiceServer() = default;

  // Prototype
  virtual starpc::Error Prototype(const prototype::webworker_rpcstream::common::PrototypeRequest& req, SRPCPrototypeService_PrototypeStream* strm) = 0;
};

// SRPCPrototypeServiceHandler implements starpc::Handler for PrototypeService.
class SRPCPrototypeServiceHandler : public starpc::Handler {
 public:
  SRPCPrototypeServiceHandler(SRPCPrototypeServiceServer* impl, const std::string& service_id = "")
      : impl_(impl), service_id_(service_id.empty() ? kSRPCPrototypeServiceServiceID : service_id) {}

  const std::string& GetServiceID() const override { return service_id_; }
  std::vector<std::string> GetMethodIDs() const override;
  std::pair<bool, starpc::Error> InvokeMethod(
      const std::string& service_id,
      const std::string& method_id,
      starpc::Stream* strm) override;

 private:
  SRPCPrototypeServiceServer* impl_;
  std::string service_id_;
};

// NewSRPCPrototypeServiceHandler creates a new handler for the given implementation.
inline std::unique_ptr<SRPCPrototypeServiceHandler> NewSRPCPrototypeServiceHandler(SRPCPrototypeServiceServer* impl) {
  return std::make_unique<SRPCPrototypeServiceHandler>(impl);
}

// SRPCRegisterPrototypeService registers the server implementation with the mux.
// The returned handler must outlive the mux registration.
inline std::pair<std::unique_ptr<SRPCPrototypeServiceHandler>, starpc::Error> SRPCRegisterPrototypeService(starpc::Mux* mux, SRPCPrototypeServiceServer* impl) {
  auto handler = NewSRPCPrototypeServiceHandler(impl);
  starpc::Error err = mux->Register(handler.get());
  if (err != starpc::Error::OK) {
    return {nullptr, err};
  }
  return {std::move(handler), starpc::Error::OK};
}

// SRPCPrototypeService_PrototypeClient is the client stream for Prototype.
class SRPCPrototypeService_PrototypeClient {
 public:
  explicit SRPCPrototypeService_PrototypeClient(std::unique_ptr<starpc::Stream> strm) : strm_(std::move(strm)) {}

  starpc::Error Recv(prototype::webworker_rpcstream::common::PrototypeResponse* msg) {
    return strm_->MsgRecv(msg);
  }

  starpc::Error CloseSend() { return strm_->CloseSend(); }
  starpc::Error Close() { return strm_->Close(); }

 private:
  std::unique_ptr<starpc::Stream> strm_;
};

// SRPCPrototypeService_PrototypeStream is the server stream for Prototype.
class SRPCPrototypeService_PrototypeStream {
 public:
  explicit SRPCPrototypeService_PrototypeStream(starpc::Stream* strm) : strm_(strm) {}

  starpc::Error Send(const prototype::webworker_rpcstream::common::PrototypeResponse& msg) {
    return strm_->MsgSend(msg);
  }

  starpc::Error SendAndClose(const prototype::webworker_rpcstream::common::PrototypeResponse& msg) {
    starpc::Error err = strm_->MsgSend(msg);
    if (err != starpc::Error::OK) return err;
    return strm_->CloseSend();
  }

 private:
  starpc::Stream* strm_;
};

}  // namespace prototype::webworker_rpcstream::common
