// @generated by protoc-gen-es-starpc none with parameter "target=ts,ts_nocheck=false"
// @generated from file github.com/s4wave/spacewave/bldr/prototypes/webworker-rpcstream/common/common.proto (package prototype.webworker_rpcstream.common, syntax proto3)
/* eslint-disable */

import { PrototypeRequest, PrototypeResponse } from './common.pb.js'
import { MethodKind } from '@aptre/protobuf-es-lite'
import { buildDecodeMessageTransform, MessageStream, ProtoRpc } from 'starpc'

/**
 * PrototypeService is a service for the prototype (regression test).
 * app1 contacts app2 and starts a streaming rpc.
 * if app1 cancels the rpc, app2 context should also cancel.
 *
 * @generated from service prototype.webworker_rpcstream.common.PrototypeService
 */
export const PrototypeServiceDefinition = {
  typeName: 'prototype.webworker_rpcstream.common.PrototypeService',
  methods: {
    /**
     * Prototype is the prototype request.
     *
     * @generated from rpc prototype.webworker_rpcstream.common.PrototypeService.Prototype
     */
    Prototype: {
      name: 'Prototype',
      I: PrototypeRequest,
      O: PrototypeResponse,
      kind: MethodKind.ServerStreaming,
    },
  },
} as const

/**
 * PrototypeService is a service for the prototype (regression test).
 * app1 contacts app2 and starts a streaming rpc.
 * if app1 cancels the rpc, app2 context should also cancel.
 *
 * @generated from service prototype.webworker_rpcstream.common.PrototypeService
 */
export interface PrototypeService {
  /**
   * Prototype is the prototype request.
   *
   * @generated from rpc prototype.webworker_rpcstream.common.PrototypeService.Prototype
   */
  Prototype(
    request: PrototypeRequest,
    abortSignal?: AbortSignal,
  ): MessageStream<PrototypeResponse>
}

export const PrototypeServiceServiceName = PrototypeServiceDefinition.typeName

export class PrototypeServiceClient implements PrototypeService {
  private readonly rpc: ProtoRpc
  private readonly service: string
  constructor(rpc: ProtoRpc, opts?: { service?: string }) {
    this.service = opts?.service || PrototypeServiceServiceName
    this.rpc = rpc
    this.Prototype = this.Prototype.bind(this)
  }
  /**
   * Prototype is the prototype request.
   *
   * @generated from rpc prototype.webworker_rpcstream.common.PrototypeService.Prototype
   */
  Prototype(
    request: PrototypeRequest,
    abortSignal?: AbortSignal,
  ): MessageStream<PrototypeResponse> {
    const requestMsg = PrototypeRequest.create(request)
    const result = this.rpc.serverStreamingRequest(
      this.service,
      PrototypeServiceDefinition.methods.Prototype.name,
      PrototypeRequest.toBinary(requestMsg),
      abortSignal || undefined,
    )
    return buildDecodeMessageTransform(PrototypeResponse)(result)
  }
}
