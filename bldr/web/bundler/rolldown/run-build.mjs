// @bun
// Code generated by bun run gen; DO NOT EDIT.

// bldr/web/bundler/rolldown/run-build.ts
import { createHash } from "crypto";
import { existsSync, readFileSync, realpathSync, statSync } from "fs";
import { promises as fs } from "fs";
import { gzipSync } from "zlib";
import {
  basename,
  dirname,
  isAbsolute,
  join,
  normalize,
  posix,
  relative,
  resolve,
  sep
} from "path";
import { pathToFileURL } from "url";
var FORMAT_VALUES = new Set(["es", "cjs", "iife"]);
var PLATFORM_VALUES = new Set(["browser", "node", "neutral"]);
var SOURCEMAP_VALUES = new Set(["none", "inline", "external", "both"]);
var LOADER_VALUES = new Set([
  "js",
  "jsx",
  "ts",
  "tsx",
  "json",
  "text",
  "dataurl",
  "base64",
  "binary",
  "asset"
]);
var DIST_SOURCE_PREFIXES = [
  "devtool/",
  "manifest/",
  "plugin/",
  "resource/",
  "sdk/",
  "web/"
];
var LOCAL_MODULE_PREFIX = "github.com/s4wave/spacewave/";
var NODE_EVENTS_ID = "\x00goscript-node-events";
var CSS_IMPORT_PATTERN = /\.(?:css|less|sass|scss|styl|stylus|pcss|postcss)(?:[?#].*)?$/i;
var IMPORT_SPECIFIER_PATTERN = /(?:\b(?:import|export)\s+(?:[^'"]*?\sfrom\s*)?|\bimport\s*\(\s*|\brequire\s*\(\s*)['"]([^'"]+)['"]/g;
function sourceHasCssImport(code) {
  IMPORT_SPECIFIER_PATTERN.lastIndex = 0;
  for (const match of code.matchAll(IMPORT_SPECIFIER_PATTERN)) {
    if (CSS_IMPORT_PATTERN.test(match[1] ?? ""))
      return true;
  }
  return false;
}

class BuildRequestError extends Error {
  diagnostic;
  constructor(message) {
    super(message);
    this.name = "BuildRequestError";
    this.diagnostic = { severity: "error", message };
  }
}
function validateBuildRequest(request) {
  const format = request.format ?? "";
  if (format && !FORMAT_VALUES.has(format)) {
    throw new BuildRequestError(`unsupported format ${JSON.stringify(format)}`);
  }
  if (format === "iife" && !request.globalName) {
    throw new BuildRequestError("format iife requires global_name");
  }
  const platform = request.platform ?? "";
  if (platform && !PLATFORM_VALUES.has(platform)) {
    throw new BuildRequestError(`unsupported platform ${JSON.stringify(platform)}`);
  }
  const sourcemap = request.sourcemap ?? "";
  if (sourcemap && !SOURCEMAP_VALUES.has(sourcemap)) {
    throw new BuildRequestError(`unsupported sourcemap ${JSON.stringify(sourcemap)}`);
  }
  for (const [extension, loader] of Object.entries(request.loaders ?? {})) {
    if (extension.toLowerCase().endsWith(".css") || loader === "css") {
      throw new BuildRequestError("CSS loader configuration is not supported by Rolldown owner");
    }
    if (!LOADER_VALUES.has(loader)) {
      throw new BuildRequestError(`unsupported loader ${JSON.stringify(loader)}`);
    }
  }
}
async function runBuild(request, dependencyRoot) {
  validateBuildRequest(request);
  const workingDir = resolve(request.workingDir || process.cwd());
  const sourceRoot = resolve(workingDir, request.sourceRoot || ".");
  const outputRoot = resolve(workingDir, request.outputRoot || "dist");
  const format = request.format || "es";
  const platform = request.platform || "browser";
  const sourcemap = request.sourcemap || "none";
  const entrypoints = request.entrypoints ?? [];
  const inputs = new Set;
  const diagnostics = [];
  const inputEntries = {};
  let hasCssImports = false;
  const virtualModules = new Map;
  const canonicalPath = (filePath) => {
    const absolute = normalize(resolve(workingDir, filePath));
    try {
      return normalize(realpathSync.native(absolute));
    } catch {
      return absolute;
    }
  };
  const sourceOverrides = new Map(Object.entries(request.sourceOverrides ?? {}).map(([filePath, source]) => [
    canonicalPath(filePath),
    source
  ]));
  const injectedPaths = (request.inject ?? []).map(canonicalPath);
  const entryInjects = new Map;
  const entrypointPaths = new Set;
  for (const entrypoint of entrypoints) {
    const name = entrypoint.name || basename(entrypoint.inputPath || "entry");
    if (!entrypoint.inputPath) {
      throw new BuildRequestError(`entrypoint ${JSON.stringify(name)} has no input_path`);
    }
    const inputPath = canonicalPath(entrypoint.inputPath);
    entrypointPaths.add(inputPath);
    if (injectedPaths.length > 0) {
      entryInjects.set(inputPath, injectedPaths);
    }
    inputEntries[name] = inputPath;
  }
  for (const [name, source] of Object.entries(request.virtualModules ?? {})) {
    virtualModules.set(name, source);
  }
  const dependencyModulesRoot = canonicalPath(join(dependencyRoot, "node_modules"));
  const trackInput = (filePath) => {
    if (!filePath || filePath.startsWith("\x00") || !isAbsolute(filePath))
      return;
    if (!existsSync(filePath))
      return;
    let normalizedPath;
    try {
      if (!statSync(filePath).isFile())
        return;
      normalizedPath = normalize(realpathSync.native(filePath));
    } catch {
      normalizedPath = normalize(filePath);
    }
    const dependencyRelative = relative(dependencyModulesRoot, normalizedPath);
    if (dependencyRelative === "" || !isAbsolute(dependencyRelative) && dependencyRelative !== ".." && !dependencyRelative.startsWith(`..${sep}`)) {
      return;
    }
    inputs.add(normalizedPath);
  };
  const existingFile = (filePath) => {
    try {
      return statSync(filePath).isFile();
    } catch {
      return false;
    }
  };
  const existingSourcePath = (filePath) => {
    if (existingFile(filePath))
      return filePath;
    if (filePath.endsWith(".js")) {
      for (const extension of [".ts", ".tsx"]) {
        const candidate = filePath.slice(0, -3) + extension;
        if (existingFile(candidate))
          return candidate;
      }
    }
    return null;
  };
  const existingTypeScriptSibling = (filePath) => {
    if (filePath.endsWith(".js")) {
      const candidate = filePath.slice(0, -3) + ".ts";
      if (existingFile(candidate))
        return candidate;
    }
    return existingFile(filePath) ? filePath : null;
  };
  const localModule = readLocalModule(sourceRoot);
  const goscript = request.goscript;
  const goScriptOutputRoot = resolve(workingDir, goscript?.outputRoot || outputRoot);
  const bldrDistRoot = canonicalPath(request.bldrDistRoot || sourceRoot);
  for (const configFile of [
    join(bldrDistRoot, "dist", "deps", "package.json"),
    join(bldrDistRoot, "dist", "deps", "bun.lock"),
    join(bldrDistRoot, "bldr", "dist", "deps", "package.json"),
    join(bldrDistRoot, "bldr", "dist", "deps", "bun.lock")
  ]) {
    trackInput(configFile);
  }
  const resolveBldrSourcePath = (sourceRel) => {
    return existingSourcePath(join(bldrDistRoot, "bldr", sourceRel)) ?? existingSourcePath(join(bldrDistRoot, sourceRel));
  };
  const resolveBldrAlias = (source) => {
    if (source === "@aptre/bldr-sdk")
      return resolveBldrSourcePath("sdk/plugin.ts");
    const sdkPrefix = "@aptre/bldr-sdk/";
    if (source.startsWith(sdkPrefix)) {
      return resolveBldrSourcePath(join("sdk", source.slice(sdkPrefix.length)));
    }
    if (source === "@aptre/bldr")
      return resolveBldrSourcePath("web/bldr/index.js");
    if (source === "@aptre/bldr-react") {
      return resolveBldrSourcePath("web/bldr-react/index.js");
    }
    return null;
  };
  const resolveGoImport = (source) => {
    if (!source.startsWith("@go/") || !source.endsWith(".js"))
      return null;
    const importPath = source.slice("@go/".length);
    if (localModule && importPath.startsWith(`${localModule.name}/`)) {
      return existingSourcePath(join(localModule.root, importPath.slice(localModule.name.length + 1)));
    }
    if (!localModule && importPath.startsWith(LOCAL_MODULE_PREFIX)) {
      return existingSourcePath(join(sourceRoot, importPath.slice(LOCAL_MODULE_PREFIX.length)));
    }
    for (const root of [localModule?.root, bldrDistRoot]) {
      if (!root)
        continue;
      const resolved = existingSourcePath(join(root, "vendor", importPath));
      if (resolved)
        return resolved;
    }
    return null;
  };
  const resolveDistSourceImport = (source) => {
    if (!source.endsWith(".js"))
      return null;
    if (!DIST_SOURCE_PREFIXES.some((prefix) => source.startsWith(prefix))) {
      return null;
    }
    return existingSourcePath(join(bldrDistRoot, source));
  };
  const resolveEscapedRelativeImport = (source, importer) => {
    if (!importer || importer.startsWith("\x00") || !source.endsWith(".js") || !source.startsWith("./") && !source.startsWith("../")) {
      return null;
    }
    const relativeImporter = relative(bldrDistRoot, importer);
    if (relativeImporter === ".." || relativeImporter.startsWith(`..${sep}`) || isAbsolute(relativeImporter)) {
      return null;
    }
    const target = normalize(join(dirname(importer), source));
    const relativeTarget = relative(bldrDistRoot, target);
    if (relativeTarget !== ".." && !relativeTarget.startsWith(`..${sep}`)) {
      return null;
    }
    const moduleImporter = posix.join(LOCAL_MODULE_PREFIX, "bldr", relativeImporter.split(sep).join("/"));
    const modulePath = posix.normalize(posix.join(posix.dirname(moduleImporter), source));
    return resolveGoImport(`@go/${modulePath}`);
  };
  const isConfiguredExternal = (source) => (request.external ?? []).some((specifier) => source === specifier || source.startsWith(`${specifier}/`));
  const configuredAliases = Object.entries(request.aliases ?? {});
  const prefixAliases = Object.entries(request.prefixAliases ?? {}).sort(([left], [right]) => right.length - left.length);
  const resolveConfiguredAlias = (source) => {
    for (const [specifier, target] of configuredAliases) {
      if (source === specifier)
        return existingSourcePath(canonicalPath(target));
    }
    for (const [prefix, aliasRoot] of prefixAliases) {
      if (!source.startsWith(prefix))
        continue;
      const rest = source.slice(prefix.length).replace(/^[/\\]+/, "");
      const resolved = existingSourcePath(join(canonicalPath(aliasRoot), rest));
      if (resolved)
        return resolved;
    }
    return null;
  };
  const sharedImportURL = (relativePath) => {
    const prefix = goscript?.sharedImportUrlPrefix || "";
    return `${prefix}${relativePath.slice(0, -3)}.mjs`;
  };
  const sharedGoScriptRel = (relativePath) => relativePath !== "" && !relativePath.startsWith("github.com/s4wave/");
  const resolveSharedGoScriptImport = (source, importer) => {
    if (!goscript?.sharedExternalImports)
      return null;
    if (source.startsWith("@goscript/")) {
      const rel = source.slice("@goscript/".length);
      if (!rel.endsWith(".js") || !sharedGoScriptRel(rel))
        return null;
      return sharedImportURL(rel);
    }
    if (!importer || importer.startsWith("\x00") || !source.endsWith(".js") || !source.startsWith("./") && !source.startsWith("../")) {
      return null;
    }
    const outputRoot = join(goScriptOutputRoot, "@goscript");
    const targetPath = normalize(join(dirname(importer), source));
    const rel = relative(outputRoot, targetPath).split(sep).join("/");
    if (rel === "" || rel.startsWith("..") || isAbsolute(rel))
      return null;
    if (!sharedGoScriptRel(rel) || !existingTypeScriptSibling(targetPath))
      return null;
    return sharedImportURL(rel);
  };
  const resolveGoScriptOverrideSourceImport = (source, importer) => {
    if (!importer || importer.startsWith("\x00") || !source.endsWith(".js") || !source.startsWith("./") && !source.startsWith("../")) {
      return null;
    }
    const outputRoot = join(goScriptOutputRoot, "@goscript");
    const targetPath = normalize(join(dirname(importer), source));
    const rel = relative(outputRoot, targetPath);
    if (rel === "" || rel.startsWith("..") || isAbsolute(rel))
      return null;
    return existingSourcePath(join(sourceRoot, "vendor", "github.com", "s4wave", "goscript", "gs", rel));
  };
  const internalResolver = {
    name: "bldr-internal-resolver",
    buildStart() {
      for (const input of Object.values(inputEntries))
        trackInput(input);
      for (const input of entrypointPaths)
        trackInput(input);
      for (const input of injectedPaths)
        trackInput(input);
      for (const input of sourceOverrides.keys())
        trackInput(input);
    },
    resolveId(source, importer) {
      if (virtualModules.has(source))
        return `\x00virtual:${source}`;
      if (goscript && source === "node:events")
        return NODE_EVENTS_ID;
      if (isConfiguredExternal(source)) {
        return { id: source, external: true };
      }
      const configuredAlias = resolveConfiguredAlias(source);
      if (configuredAlias) {
        trackInput(configuredAlias);
        return configuredAlias;
      }
      const bldrAlias = resolveBldrAlias(source);
      if (bldrAlias) {
        trackInput(bldrAlias);
        return bldrAlias;
      }
      const goImport = resolveGoImport(source);
      if (goImport) {
        trackInput(goImport);
        return goImport;
      }
      const distSourceImport = resolveDistSourceImport(source);
      if (distSourceImport) {
        trackInput(distSourceImport);
        return distSourceImport;
      }
      const escapedRelativeImport = resolveEscapedRelativeImport(source, importer);
      if (escapedRelativeImport) {
        trackInput(escapedRelativeImport);
        return escapedRelativeImport;
      }
      if (request.externalPackages && isBarePackageImport(source)) {
        return { id: source, external: true };
      }
      if (!goscript)
        return null;
      if (source.startsWith("@goscript/")) {
        const sharedImport = resolveSharedGoScriptImport(source, importer);
        if (sharedImport)
          return { id: sharedImport, external: true };
        const resolved = existingTypeScriptSibling(join(goScriptOutputRoot, "@goscript", source.slice("@goscript/".length)));
        if (resolved) {
          trackInput(resolved);
          return resolved;
        }
        return null;
      }
      if (importer && !importer.startsWith("\x00") && source.endsWith(".js") && (source.startsWith("./") || source.startsWith("../"))) {
        const sharedImport = resolveSharedGoScriptImport(source, importer);
        if (sharedImport)
          return { id: sharedImport, external: true };
        const resolved = existingTypeScriptSibling(join(dirname(importer), source));
        if (resolved) {
          trackInput(resolved);
          return resolved;
        }
        const overrideSource = resolveGoScriptOverrideSourceImport(source, importer);
        if (overrideSource) {
          trackInput(overrideSource);
          return overrideSource;
        }
      }
      return null;
    },
    load(id) {
      if (id === NODE_EVENTS_ID)
        return `export function setMaxListeners() {}
`;
      if (id.startsWith("\x00virtual:"))
        return virtualModules.get(id.slice(9)) ?? null;
      const normalizedID = normalize(id);
      trackInput(normalizedID);
      const overrideSource = sourceOverrides.get(normalizedID);
      const injects = entryInjects.get(normalizedID);
      if (!injects)
        return overrideSource ?? null;
      const original = overrideSource ?? readFileSync(normalizedID, "utf8");
      const imports = injects.map((filePath) => `import ${JSON.stringify(filePath)};`).join(`
`);
      return `${imports}
${original}`;
    },
    transform(code, id) {
      if (request.routeCssImports && sourceHasCssImport(code)) {
        hasCssImports = true;
        trackInput(id);
      }
      return null;
    }
  };
  const plugins = [
    {
      name: "virtual-modules",
      resolveId(source) {
        return virtualModules.has(source) ? `\x00virtual:${source}` : null;
      },
      load(id) {
        return id.startsWith("\x00virtual:") ? virtualModules.get(id.slice(9)) ?? null : null;
      }
    },
    internalResolver
  ];
  if (!request.sourcemap || request.sourcemap === "none") {
    plugins.unshift({
      name: "strip-code-regions",
      renderChunk(code) {
        return code.replace(/^\/\/#(?:end)?region.*(?:\r?\n|$)/gm, "");
      }
    });
  }
  if (request.publicPath) {
    plugins.push({
      name: "public-path",
      resolveFileUrl({ fileName }) {
        return JSON.stringify(joinURL(request.publicPath || "", fileName));
      }
    });
  }
  const recordLog = (level, log) => {
    const loc = log.loc;
    const file = loc?.file || log.id || "";
    const line = loc?.line || 0;
    const column = loc?.column || 0;
    let lineText = "";
    if (file && line > 0 && existsSync(file)) {
      try {
        lineText = readFileSync(file, "utf8").split(/\r?\n/)[line - 1] || "";
      } catch {
        lineText = "";
      }
    }
    diagnostics.push({
      severity: level === "warn" ? "warning" : level,
      message: log.message || log.code || "Rolldown diagnostic",
      code: log.code || "",
      file,
      line,
      column,
      lineText
    });
  };
  const isUndefinedImport = (log) => {
    const message = log.message || "";
    return log.code === "IMPORT_IS_UNDEFINED" || log.code === "import-is-undefined" || log.code === "ImportIsUndefined" || message.includes("will always be undefined because there is no matching export");
  };
  const outputSourcemap = sourcemap === "none" || sourcemap === "both" ? sourcemap === "both" ? true : false : sourcemap === "inline" ? "inline" : true;
  const options = {
    input: inputEntries,
    cwd: workingDir,
    write: true,
    platform,
    external: [...request.external ?? []],
    treeshake: request.treeShaking ?? true,
    checks: { importIsUndefined: true },
    moduleTypes: { ...request.loaders ?? {} },
    resolve: {
      alias: { ...request.aliases ?? {} },
      extensionAlias: { ".js": [".ts", ".tsx", ".js"] },
      modules: [join(dependencyRoot, "node_modules")]
    },
    transform: {
      target: request.target || undefined,
      define: { ...request.defines ?? {} }
    },
    plugins,
    onLog(level, log, _defaultHandler) {
      recordLog(level, log);
      if (isUndefinedImport(log)) {
        const error = new Error(`undefined GoScript import${log.id ? ` in ${log.id}` : ""}: ${log.message || log.code || "missing export"}`);
        error.diagnostic = {
          ...diagnostics[diagnostics.length - 1] ?? {},
          severity: "error",
          message: error.message
        };
        throw error;
      }
    },
    logLevel: "warn",
    output: {
      dir: outputRoot,
      format,
      name: request.globalName || undefined,
      entryFileNames: request.entryFileNames || "[name].js",
      chunkFileNames: request.chunkFileNames || "[name]-[hash].js",
      assetFileNames: request.assetFileNames || "[name]-[hash][extname]",
      sourcemap: outputSourcemap,
      codeSplitting: request.codeSplitting ?? false,
      minify: request.minify ? { compress: true, mangle: true } : false,
      comments: false,
      banner: request.banner || undefined,
      cleanDir: request.cleanOutputDir ?? false
    }
  };
  try {
    await fs.mkdir(outputRoot, { recursive: true });
    const modulePath = join(dependencyRoot, "node_modules", "rolldown", "dist", "index.mjs");
    const moduleNamespace = await import(pathToFileURL(modulePath).href);
    const output = await moduleNamespace.build(options);
    await appendBothSourcemaps(outputRoot, output.output, sourcemap === "both");
    const buildOutputs = await collectOutputs(outputRoot, output.output, entrypoints);
    const tool = await identifyTool(dependencyRoot);
    return {
      inputs: [...inputs].sort(),
      outputs: buildOutputs,
      entrypointOutputs: entrypointOutputMap(buildOutputs),
      tool,
      diagnostics,
      hasCssImports
    };
  } catch (value) {
    const error = toError(value);
    if (request.routeCssImports && hasCssImports) {
      return {
        inputs: [...inputs].sort(),
        outputs: [],
        entrypointOutputs: {},
        tool: await identifyTool(dependencyRoot),
        diagnostics,
        hasCssImports: true
      };
    }
    if (error.diagnostic)
      diagnostics.push(error.diagnostic);
    else if (!diagnostics.some((diagnostic) => diagnostic.message === error.message)) {
      diagnostics.push(diagnosticFromError(value, error));
    }
    return {
      inputs: [...inputs].sort(),
      outputs: [],
      entrypointOutputs: {},
      tool: await identifyTool(dependencyRoot),
      diagnostics,
      hasCssImports
    };
  }
}
function isBarePackageImport(source) {
  return source !== "" && !source.startsWith(".") && !source.startsWith("/") && !source.startsWith("\x00") && !/^[A-Za-z]:[\\/]/.test(source);
}
function readLocalModule(sourceRoot) {
  let root = resolve(sourceRoot);
  for (;; ) {
    let contents;
    try {
      contents = readFileSync(join(root, "go.mod"), "utf8");
    } catch {
      const parent = dirname(root);
      if (parent === root)
        return null;
      root = parent;
      continue;
    }
    const name = contents.match(/^\s*module\s+(\S+)/m)?.[1] || "";
    if (name)
      return { name, root };
    const parent = dirname(root);
    if (parent === root)
      return null;
    root = parent;
  }
}
function joinURL(prefix, fileName) {
  if (!prefix)
    return fileName;
  return `${prefix.replace(/\/$/, "")}/${fileName.replace(/^\//, "")}`;
}
async function appendBothSourcemaps(outputRoot, outputs, enabled) {
  if (!enabled)
    return;
  for (const output of outputs) {
    if (output.type !== "chunk")
      continue;
    const mapName = output.sourcemapFileName || `${output.fileName}.map`;
    const mapPath = join(outputRoot, mapName);
    if (!existsSync(mapPath))
      continue;
    const mapBytes = await fs.readFile(mapPath);
    const outputPath = join(outputRoot, output.fileName);
    const contents = await fs.readFile(outputPath, "utf8");
    const body = contents.replace(/\s*\/\/# sourceMappingURL=.*$/s, "").replace(/\s*$/, "");
    await fs.writeFile(outputPath, `${body}
//# sourceMappingURL=data:application/json;base64,${mapBytes.toString("base64")}
`);
  }
}
async function collectOutputs(outputRoot, outputs, entrypoints) {
  const result = [];
  for (const output of outputs) {
    const path = normalizeOutputPath(outputRoot, output.fileName);
    const contents = await fs.readFile(join(outputRoot, path));
    const type = output.fileName.endsWith(".map") ? "map" : output.type === "chunk" ? "javascript" : "asset";
    const entrypointName = output.type === "chunk" && output.isEntry ? entrypointForFacade(output.facadeModuleId, entrypoints) : "";
    result.push({
      path,
      type,
      entrypointName,
      bytes: BigInt(contents.byteLength),
      gzipBytes: BigInt(gzipSync(contents, { level: 9 }).byteLength),
      sha256: createHash("sha256").update(contents).digest("hex")
    });
  }
  return result.sort((left, right) => (left.path ?? "").localeCompare(right.path ?? ""));
}
function normalizeOutputPath(outputRoot, outputPath) {
  const absolute = resolve(outputRoot, outputPath);
  const relativePath = normalize(relative(outputRoot, absolute));
  if (relativePath === ".." || relativePath.startsWith(`..${sep}`) || isAbsolute(relativePath)) {
    throw new Error(`Rolldown emitted output outside output root: ${outputPath}`);
  }
  return relativePath.split(sep).join("/");
}
function entrypointForFacade(facade, entrypoints) {
  if (!facade)
    return "";
  const normalizedFacade = normalizeExistingPath(facade);
  return entrypoints.find((entrypoint) => normalizeExistingPath(entrypoint.inputPath || "") === normalizedFacade)?.name || "";
}
function normalizeExistingPath(filePath) {
  if (!filePath)
    return "";
  const absolute = normalize(resolve(filePath));
  try {
    return normalize(realpathSync.native(absolute));
  } catch {
    return absolute;
  }
}
function entrypointOutputMap(outputs) {
  const result = {};
  for (const output of outputs) {
    if (output.entrypointName && output.path && output.type === "javascript") {
      result[output.entrypointName] = output.path;
    }
  }
  return result;
}
async function identifyTool(dependencyRoot) {
  let rolldownVersion = "";
  try {
    const packageJSON = JSON.parse(await fs.readFile(join(dependencyRoot, "node_modules", "rolldown", "package.json"), "utf8"));
    rolldownVersion = packageJSON.version || "";
  } catch {
    rolldownVersion = "";
  }
  return {
    rolldownVersion,
    bunVersion: process.versions.bun ?? "",
    platform: process.platform,
    arch: process.arch
  };
}
function toError(value) {
  if (value instanceof Error)
    return value;
  return Object.assign(new Error(String(value)), { diagnostic: undefined });
}
function diagnosticFromError(value, error) {
  const candidate = typeof value === "object" && value !== null ? value : undefined;
  const log = candidate;
  const loc = log?.loc;
  return {
    severity: "error",
    message: error.message,
    code: log?.code || "",
    file: loc?.file || log?.id || "",
    line: loc?.line || 0,
    column: loc?.column || 0,
    lineText: log?.frame || ""
  };
}
function requestFromJSON(value) {
  const input = value && typeof value === "object" ? value : {};
  const read = (camel, snake) => input[snake] ?? input[camel];
  const entrypoints = read("entrypoints", "entrypoints")?.map((entrypoint) => {
    const item = entrypoint;
    return {
      name: item.name || "",
      inputPath: item.input_path ?? item.inputPath ?? ""
    };
  });
  return {
    workingDir: read("workingDir", "working_dir"),
    sourceRoot: read("sourceRoot", "source_root"),
    outputRoot: read("outputRoot", "output_root"),
    entrypoints,
    format: read("format", "format"),
    globalName: read("globalName", "global_name"),
    platform: read("platform", "platform"),
    target: read("target", "target"),
    entryFileNames: read("entryFileNames", "entry_file_names"),
    chunkFileNames: read("chunkFileNames", "chunk_file_names"),
    assetFileNames: read("assetFileNames", "asset_file_names"),
    publicPath: read("publicPath", "public_path"),
    codeSplitting: read("codeSplitting", "code_splitting"),
    sourcemap: read("sourcemap", "sourcemap"),
    minify: read("minify", "minify"),
    treeShaking: read("treeShaking", "tree_shaking"),
    banner: read("banner", "banner"),
    defines: read("defines", "defines"),
    external: read("external", "external"),
    aliases: read("aliases", "aliases"),
    loaders: read("loaders", "loaders"),
    virtualModules: read("virtualModules", "virtual_modules"),
    goscript: (() => {
      const policy = read("goscript", "goscript");
      if (!policy)
        return;
      return {
        outputRoot: policy.output_root ?? policy.outputRoot ?? "",
        sharedExternalImports: policy.shared_external_imports ?? policy.sharedExternalImports ?? false,
        sharedImportUrlPrefix: policy.shared_import_url_prefix ?? policy.sharedImportUrlPrefix ?? ""
      };
    })(),
    inject: read("inject", "inject"),
    bldrDistRoot: read("bldrDistRoot", "bldr_dist_root"),
    cleanOutputDir: read("cleanOutputDir", "clean_output_dir"),
    externalPackages: read("externalPackages", "external_packages"),
    sourceOverrides: read("sourceOverrides", "source_overrides"),
    prefixAliases: read("prefixAliases", "prefix_aliases"),
    routeCssImports: read("routeCssImports", "route_css_imports")
  };
}
function resultToJSON(result) {
  return {
    inputs: result.inputs ?? [],
    outputs: (result.outputs ?? []).map((output) => ({
      path: output.path || "",
      type: output.type || "",
      entrypoint_name: output.entrypointName || "",
      bytes: String(output.bytes ?? 0n),
      gzip_bytes: String(output.gzipBytes ?? 0n),
      sha256: output.sha256 || ""
    })),
    entrypoint_outputs: result.entrypointOutputs ?? {},
    tool: {
      rolldown_version: result.tool?.rolldownVersion || "",
      bun_version: result.tool?.bunVersion || "",
      platform: result.tool?.platform || "",
      arch: result.tool?.arch || ""
    },
    diagnostics: (result.diagnostics ?? []).map((diagnostic) => ({
      severity: diagnostic.severity || "",
      message: diagnostic.message || "",
      code: diagnostic.code || "",
      file: diagnostic.file || "",
      line: diagnostic.line || 0,
      column: diagnostic.column || 0,
      line_text: diagnostic.lineText || ""
    })),
    has_css_imports: result.hasCssImports ?? false
  };
}
function failureResult(value) {
  const error = toError(value);
  return {
    inputs: [],
    outputs: [],
    entrypointOutputs: {},
    tool: {
      rolldownVersion: "",
      bunVersion: process.versions.bun ?? "",
      platform: process.platform,
      arch: process.arch
    },
    diagnostics: [error.diagnostic || diagnosticFromError(value, error)]
  };
}
async function main() {
  const [, , requestPath, resultPath, dependencyRoot] = process.argv;
  if (!requestPath || !resultPath || !dependencyRoot) {
    throw new BuildRequestError("usage: run-build.ts <request.json> <result.json> <dependency-root>");
  }
  let result;
  try {
    const request = requestFromJSON(JSON.parse(await fs.readFile(requestPath, "utf8")));
    result = await runBuild(request, dependencyRoot);
  } catch (value) {
    result = failureResult(value);
  }
  await fs.writeFile(resultPath, `${JSON.stringify(resultToJSON(result), null, 2)}
`);
  if ((result.diagnostics ?? []).some((diagnostic) => diagnostic.severity === "error")) {
    process.exitCode = 1;
  }
}
if (import.meta.main) {
  main().catch((error) => {
    process.exitCode = 1;
    console.error(error);
  });
}
export {
  BuildRequestError,
  runBuild,
  validateBuildRequest
};
