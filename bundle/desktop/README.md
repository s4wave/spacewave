# Desktop Bundling

This package bundles the compiled version into a desktop app with Electron.

Webpack is used to compile the React app to a bundle. Electron-Webpack is then used to build that bundle into the Electron distributions.
