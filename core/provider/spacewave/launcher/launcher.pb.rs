// @generated
// This file is @generated by prost-build.
/// DistConfig is the encoded and signed object that is exposed on the
/// distribution URL endpoints. The config with the highest valid revision field
/// and valid signature is accepted as the latest config.
///
/// Usually encoded with packedmsg(peer.SignedMsg(DistConfig))
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct DistConfig {
    /// ProjectId is the identifier of the project.
    /// usually matches the id: field in the project config.
    /// if this doesn't match the app config is ignored.
    #[prost(string, tag="1")]
    pub project_id: ::prost::alloc::string::String,
    /// Rev is the revision of this app config object.
    #[prost(uint64, tag="2")]
    pub rev: u64,
    /// LauncherConfigSet is a ConfigSet to apply to the launcher.
    /// This ConfigSet is applied to the launcher plugin bus.
    /// Note: the launcher configuration itself could be overridden by this.
    /// Note: the highest revision config is used in the configset controller.
    #[prost(map="string, message", tag="3")]
    pub launcher_config_set: ::std::collections::HashMap<::prost::alloc::string::String, super::super::configset::proto::ControllerConfig>,
    /// EntrypointVersion is the latest entrypoint binary version.
    #[prost(string, tag="4")]
    pub entrypoint_version: ::prost::alloc::string::String,
    /// EntrypointAssets maps platform/arch (e.g. "darwin/arm64") to the
    /// download URL for the new entrypoint binary.
    #[prost(map="string, message", tag="5")]
    pub entrypoint_assets: ::std::collections::HashMap<::prost::alloc::string::String, EntrypointAsset>,
    /// Channel selects the release channel this DistConfig targets.
    /// Empty means "stable"; alpha clients that do not know an advertised
    /// channel MUST treat the DistConfig as stable (forward-compat fallback).
    /// Reserved future values: "beta", "canary", "internal".
    #[prost(string, tag="8")]
    pub channel: ::prost::alloc::string::String,
}
/// EntrypointAsset describes a downloadable entrypoint binary for a platform.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct EntrypointAsset {
    /// Url is the download URL for the binary.
    #[prost(string, tag="1")]
    pub url: ::prost::alloc::string::String,
    /// Size is the expected file size in bytes.
    #[prost(uint64, tag="2")]
    pub size: u64,
    /// Sha256 is the SHA-256 hash of the binary.
    #[prost(bytes="vec", tag="3")]
    pub sha256: ::prost::alloc::vec::Vec<u8>,
}
/// LauncherInfo contains information about the state of the launcher.
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct LauncherInfo {
    /// DistConfig contains the latest app dist config object.
    /// May be empty if the config is not known / fetched yet.
    #[prost(message, optional, tag="1")]
    pub dist_config: ::core::option::Option<DistConfig>,
    /// UpdateState describes the entrypoint update state.
    #[prost(message, optional, tag="2")]
    pub update_state: ::core::option::Option<UpdateState>,
}
/// UpdateState describes the current state of an entrypoint self-update.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct UpdateState {
    /// Phase is the current update phase.
    #[prost(enumeration="UpdatePhase", tag="1")]
    pub phase: i32,
    /// Version is the target update version.
    #[prost(string, tag="2")]
    pub version: ::prost::alloc::string::String,
    /// DownloadProgress is download progress percentage (0-100).
    #[prost(uint32, tag="3")]
    pub download_progress: u32,
    /// StagedPath is the path to the staged binary (when phase is STAGED).
    #[prost(string, tag="4")]
    pub staged_path: ::prost::alloc::string::String,
    /// ErrorMessage contains any error message.
    #[prost(string, tag="5")]
    pub error_message: ::prost::alloc::string::String,
}
/// StagedManifest is a sidecar manifest written into the staging directory
/// after a .app bundle has been extracted and codesign-verified. The launcher
/// reads it at boot to decide whether the on-disk STAGED .app is still fresh
/// against the latest DistConfig. The manifest is binary-serialized proto
/// written to =manifest.binpb= in the staging dir.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct StagedManifest {
    /// Version is the entrypoint version of the staged .app.
    #[prost(string, tag="1")]
    pub version: ::prost::alloc::string::String,
    /// Path is the absolute path to the staged =Spacewave-<version>.app=.
    #[prost(string, tag="2")]
    pub path: ::prost::alloc::string::String,
    /// SignatureHash is the SHA-256 hash of the archive that produced the .app,
    /// copied from =EntrypointAsset.sha256=. Used to detect resumed-and-tampered
    /// staging trees where the archive was swapped out after extract.
    #[prost(bytes="vec", tag="3")]
    pub signature_hash: ::prost::alloc::vec::Vec<u8>,
}
/// RecheckDistConfigRequest is a request to immediately recheck for updates.
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct RecheckDistConfigRequest {
}
/// RecheckDistConfigRequest is a response to immediately recheck for updates.
/// Returned immediately
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct RecheckDistConfigResponse {
}
/// WatchLauncherInfoRequest is the request to get the launcher info.
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct WatchLauncherInfoRequest {
}
/// PushDistConfigRequest is the request to push an app dist config signed message.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct PushDistConfigRequest {
    /// Body is the block of text containing the packedmsg.
    /// We will scan the body of message for a valid signed packedmsg.
    #[prost(string, tag="1")]
    pub body: ::prost::alloc::string::String,
}
/// PushDistConfigResponse is the response to PushDistConfigRequest.
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct PushDistConfigResponse {
    /// Valid indicates that a valid packedmsg was found in the body.
    #[prost(bool, tag="1")]
    pub valid: bool,
    /// Updated indicates that the found packedmsg is now the latest.
    /// Will be false if valid=false.
    #[prost(bool, tag="2")]
    pub updated: bool,
    /// Rev was the revision of the found app config object. 0 if none.
    #[prost(uint64, tag="3")]
    pub rev: u64,
    /// PrevRev was the revision of the old app config object. 0 if none.
    #[prost(uint64, tag="4")]
    pub prev_rev: u64,
}
/// ApplyUpdateRequest is a request to apply a staged update.
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct ApplyUpdateRequest {
}
/// ApplyUpdateResponse is the response to ApplyUpdateRequest.
/// If successful, the process will be relaunched and this response may not arrive.
#[derive(Clone, Copy, PartialEq, Eq, Hash, ::prost::Message)]
pub struct ApplyUpdateResponse {
}
/// UpdatePhase is the phase of the entrypoint update.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum UpdatePhase {
    /// UpdatePhase_IDLE means no update in progress.
    Idle = 0,
    /// UpdatePhase_DOWNLOADING means the update is being downloaded.
    Downloading = 1,
    /// UpdatePhase_STAGED means the update is downloaded and ready to install.
    Staged = 2,
    /// UpdatePhase_APPLYING means the update is being applied.
    Applying = 3,
    /// UpdatePhase_ERROR means an error occurred.
    Error = 4,
}
impl UpdatePhase {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            Self::Idle => "UpdatePhase_IDLE",
            Self::Downloading => "UpdatePhase_DOWNLOADING",
            Self::Staged => "UpdatePhase_STAGED",
            Self::Applying => "UpdatePhase_APPLYING",
            Self::Error => "UpdatePhase_ERROR",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "UpdatePhase_IDLE" => Some(Self::Idle),
            "UpdatePhase_DOWNLOADING" => Some(Self::Downloading),
            "UpdatePhase_STAGED" => Some(Self::Staged),
            "UpdatePhase_APPLYING" => Some(Self::Applying),
            "UpdatePhase_ERROR" => Some(Self::Error),
            _ => None,
        }
    }
}
// @@protoc_insertion_point(module)
