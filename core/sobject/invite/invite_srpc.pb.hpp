//go:build deps_only && cgo

// Code generated by protoc-gen-starpc-cpp. DO NOT EDIT.
// protoc-gen-starpc-cpp version: v0.49.7
// source: github.com/s4wave/spacewave/core/sobject/invite/invite.proto

#pragma once

#include <functional>
#include <memory>
#include <string>
#include <utility>

#include "srpc/starpc.hpp"

#include "invite.pb.h"

namespace sobject::invite {

// Service ID for SOInviteService
constexpr const char* kSRPCSOInviteServiceServiceID = "sobject.invite.SOInviteService";


// SRPCSOInviteServiceClient is the client API for SOInviteService service.
class SRPCSOInviteServiceClient {
 public:
  virtual ~SRPCSOInviteServiceClient() = default;

  // SRPCClient returns the underlying SRPC client.
  virtual starpc::Client* SRPCClient() = 0;

  // AcceptInvite
  virtual starpc::Error AcceptInvite(const sobject::invite::AcceptInviteRequest& in, sobject::invite::AcceptInviteResponse* out) = 0;
};

// SRPCSOInviteServiceClientImpl implements SRPCSOInviteServiceClient.
class SRPCSOInviteServiceClientImpl : public SRPCSOInviteServiceClient {
 public:
  explicit SRPCSOInviteServiceClientImpl(starpc::Client* cc, const std::string& service_id = "")
      : cc_(cc), service_id_(service_id.empty() ? kSRPCSOInviteServiceServiceID : service_id) {}

  starpc::Client* SRPCClient() override { return cc_; }

  // AcceptInvite
  virtual starpc::Error AcceptInvite(const sobject::invite::AcceptInviteRequest& in, sobject::invite::AcceptInviteResponse* out) override;

 private:
  starpc::Client* cc_;
  std::string service_id_;
};

// NewSRPCSOInviteServiceClient creates a new client.
inline std::unique_ptr<SRPCSOInviteServiceClient> NewSRPCSOInviteServiceClient(starpc::Client* cc) {
  return std::make_unique<SRPCSOInviteServiceClientImpl>(cc);
}

// SRPCSOInviteServiceServer is the server API for SOInviteService service.
class SRPCSOInviteServiceServer {
 public:
  virtual ~SRPCSOInviteServiceServer() = default;

  // AcceptInvite
  virtual starpc::Error AcceptInvite(const sobject::invite::AcceptInviteRequest& req, sobject::invite::AcceptInviteResponse* resp) = 0;
};

// SRPCSOInviteServiceHandler implements starpc::Handler for SOInviteService.
class SRPCSOInviteServiceHandler : public starpc::Handler {
 public:
  SRPCSOInviteServiceHandler(SRPCSOInviteServiceServer* impl, const std::string& service_id = "")
      : impl_(impl), service_id_(service_id.empty() ? kSRPCSOInviteServiceServiceID : service_id) {}

  const std::string& GetServiceID() const override { return service_id_; }
  std::vector<std::string> GetMethodIDs() const override;
  std::pair<bool, starpc::Error> InvokeMethod(
      const std::string& service_id,
      const std::string& method_id,
      starpc::Stream* strm) override;

 private:
  SRPCSOInviteServiceServer* impl_;
  std::string service_id_;
};

// NewSRPCSOInviteServiceHandler creates a new handler for the given implementation.
inline std::unique_ptr<SRPCSOInviteServiceHandler> NewSRPCSOInviteServiceHandler(SRPCSOInviteServiceServer* impl) {
  return std::make_unique<SRPCSOInviteServiceHandler>(impl);
}

// SRPCRegisterSOInviteService registers the server implementation with the mux.
// The returned handler must outlive the mux registration.
inline std::pair<std::unique_ptr<SRPCSOInviteServiceHandler>, starpc::Error> SRPCRegisterSOInviteService(starpc::Mux* mux, SRPCSOInviteServiceServer* impl) {
  auto handler = NewSRPCSOInviteServiceHandler(impl);
  starpc::Error err = mux->Register(handler.get());
  if (err != starpc::Error::OK) {
    return {nullptr, err};
  }
  return {std::move(handler), starpc::Error::OK};
}

}  // namespace sobject::invite
