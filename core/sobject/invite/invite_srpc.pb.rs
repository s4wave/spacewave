// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.49.5
// source: github.com/s4wave/spacewave/core/sobject/invite/invite.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for SOInviteService.
pub const S_O_INVITE_SERVICE_SERVICE_ID: &str = "sobject.invite.SOInviteService";

/// Client trait for SOInviteService.
#[starpc::async_trait]
pub trait SOInviteServiceClient: Send + Sync {
    /// AcceptInvite.
    async fn accept_invite(&self, request: &AcceptInviteRequest) -> starpc::Result<AcceptInviteResponse>;
}

/// Client implementation for SOInviteService.
pub struct SOInviteServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> SOInviteServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> SOInviteServiceClient for SOInviteServiceClientImpl<C> {
    async fn accept_invite(&self, request: &AcceptInviteRequest) -> starpc::Result<AcceptInviteResponse> {
        self.client.exec_call("sobject.invite.SOInviteService", "AcceptInvite", request).await
    }
}

/// Server trait for SOInviteService.
#[starpc::async_trait]
pub trait SOInviteServiceServer: Send + Sync {
    /// AcceptInvite.
    async fn accept_invite(&self, request: AcceptInviteRequest) -> starpc::Result<AcceptInviteResponse>;
}

const S_O_INVITE_SERVICE_METHOD_IDS: &[&str] = &[
    "AcceptInvite",
];

/// Handler for SOInviteService.
pub struct SOInviteServiceHandler<S: SOInviteServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: SOInviteServiceServer + 'static> SOInviteServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: SOInviteServiceServer + 'static> starpc::Invoker for SOInviteServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "AcceptInvite" => {
                let request: AcceptInviteRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.accept_invite(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: SOInviteServiceServer + 'static> starpc::Handler for SOInviteServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "sobject.invite.SOInviteService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        S_O_INVITE_SERVICE_METHOD_IDS
    }
}

