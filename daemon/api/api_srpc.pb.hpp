//go:build deps_only && cgo

// Code generated by protoc-gen-starpc-cpp. DO NOT EDIT.
// protoc-gen-starpc-cpp version: v0.49.2
// source: github.com/aperturerobotics/hydra/daemon/api/api.proto

#pragma once

#include <functional>
#include <memory>
#include <string>
#include <utility>

#include "srpc/starpc.hpp"

#include "api.pb.h"

namespace hydra::api {

// Service ID for HydraDaemonService
constexpr const char* kSRPCHydraDaemonServiceServiceID = "hydra.api.HydraDaemonService";

class SRPCHydraDaemonService_ApplyBucketConfigClient;
class SRPCHydraDaemonService_ApplyBucketConfigStream;

// SRPCHydraDaemonServiceClient is the client API for HydraDaemonService service.
class SRPCHydraDaemonServiceClient {
 public:
  virtual ~SRPCHydraDaemonServiceClient() = default;

  // SRPCClient returns the underlying SRPC client.
  virtual starpc::Client* SRPCClient() = 0;

  // ListVolumes
  virtual starpc::Error ListVolumes(const hydra::api::ListVolumesRequest& in, hydra::api::ListVolumesResponse* out) = 0;
  // ListBuckets
  virtual starpc::Error ListBuckets(const volume::ListBucketsRequest& in, hydra::api::ListBucketsResponse* out) = 0;
  // ApplyBucketConfig
  virtual std::pair<std::unique_ptr<SRPCHydraDaemonService_ApplyBucketConfigClient>, starpc::Error> ApplyBucketConfig(const hydra::api::ApplyBucketConfigRequest& in) = 0;
  // BucketOp
  virtual starpc::Error BucketOp(const hydra::api::BucketOpRequest& in, hydra::api::BucketOpResponse* out) = 0;
  // ObjectStoreOp
  virtual starpc::Error ObjectStoreOp(const hydra::api::ObjectStoreOpRequest& in, hydra::api::ObjectStoreOpResponse* out) = 0;
};

// SRPCHydraDaemonServiceClientImpl implements SRPCHydraDaemonServiceClient.
class SRPCHydraDaemonServiceClientImpl : public SRPCHydraDaemonServiceClient {
 public:
  explicit SRPCHydraDaemonServiceClientImpl(starpc::Client* cc, const std::string& service_id = "")
      : cc_(cc), service_id_(service_id.empty() ? kSRPCHydraDaemonServiceServiceID : service_id) {}

  starpc::Client* SRPCClient() override { return cc_; }

  // ListVolumes
  virtual starpc::Error ListVolumes(const hydra::api::ListVolumesRequest& in, hydra::api::ListVolumesResponse* out) override;
  // ListBuckets
  virtual starpc::Error ListBuckets(const volume::ListBucketsRequest& in, hydra::api::ListBucketsResponse* out) override;
  // ApplyBucketConfig
  virtual std::pair<std::unique_ptr<SRPCHydraDaemonService_ApplyBucketConfigClient>, starpc::Error> ApplyBucketConfig(const hydra::api::ApplyBucketConfigRequest& in) override;
  // BucketOp
  virtual starpc::Error BucketOp(const hydra::api::BucketOpRequest& in, hydra::api::BucketOpResponse* out) override;
  // ObjectStoreOp
  virtual starpc::Error ObjectStoreOp(const hydra::api::ObjectStoreOpRequest& in, hydra::api::ObjectStoreOpResponse* out) override;

 private:
  starpc::Client* cc_;
  std::string service_id_;
};

// NewSRPCHydraDaemonServiceClient creates a new client.
inline std::unique_ptr<SRPCHydraDaemonServiceClient> NewSRPCHydraDaemonServiceClient(starpc::Client* cc) {
  return std::make_unique<SRPCHydraDaemonServiceClientImpl>(cc);
}

// SRPCHydraDaemonServiceServer is the server API for HydraDaemonService service.
class SRPCHydraDaemonServiceServer {
 public:
  virtual ~SRPCHydraDaemonServiceServer() = default;

  // ListVolumes
  virtual starpc::Error ListVolumes(const hydra::api::ListVolumesRequest& req, hydra::api::ListVolumesResponse* resp) = 0;
  // ListBuckets
  virtual starpc::Error ListBuckets(const volume::ListBucketsRequest& req, hydra::api::ListBucketsResponse* resp) = 0;
  // ApplyBucketConfig
  virtual starpc::Error ApplyBucketConfig(const hydra::api::ApplyBucketConfigRequest& req, SRPCHydraDaemonService_ApplyBucketConfigStream* strm) = 0;
  // BucketOp
  virtual starpc::Error BucketOp(const hydra::api::BucketOpRequest& req, hydra::api::BucketOpResponse* resp) = 0;
  // ObjectStoreOp
  virtual starpc::Error ObjectStoreOp(const hydra::api::ObjectStoreOpRequest& req, hydra::api::ObjectStoreOpResponse* resp) = 0;
};

// SRPCHydraDaemonServiceHandler implements starpc::Handler for HydraDaemonService.
class SRPCHydraDaemonServiceHandler : public starpc::Handler {
 public:
  SRPCHydraDaemonServiceHandler(SRPCHydraDaemonServiceServer* impl, const std::string& service_id = "")
      : impl_(impl), service_id_(service_id.empty() ? kSRPCHydraDaemonServiceServiceID : service_id) {}

  const std::string& GetServiceID() const override { return service_id_; }
  std::vector<std::string> GetMethodIDs() const override;
  std::pair<bool, starpc::Error> InvokeMethod(
      const std::string& service_id,
      const std::string& method_id,
      starpc::Stream* strm) override;

 private:
  SRPCHydraDaemonServiceServer* impl_;
  std::string service_id_;
};

// NewSRPCHydraDaemonServiceHandler creates a new handler for the given implementation.
inline std::unique_ptr<SRPCHydraDaemonServiceHandler> NewSRPCHydraDaemonServiceHandler(SRPCHydraDaemonServiceServer* impl) {
  return std::make_unique<SRPCHydraDaemonServiceHandler>(impl);
}

// SRPCRegisterHydraDaemonService registers the server implementation with the mux.
// The returned handler must outlive the mux registration.
inline std::pair<std::unique_ptr<SRPCHydraDaemonServiceHandler>, starpc::Error> SRPCRegisterHydraDaemonService(starpc::Mux* mux, SRPCHydraDaemonServiceServer* impl) {
  auto handler = NewSRPCHydraDaemonServiceHandler(impl);
  starpc::Error err = mux->Register(handler.get());
  if (err != starpc::Error::OK) {
    return {nullptr, err};
  }
  return {std::move(handler), starpc::Error::OK};
}

// SRPCHydraDaemonService_ApplyBucketConfigClient is the client stream for ApplyBucketConfig.
class SRPCHydraDaemonService_ApplyBucketConfigClient {
 public:
  explicit SRPCHydraDaemonService_ApplyBucketConfigClient(std::unique_ptr<starpc::Stream> strm) : strm_(std::move(strm)) {}

  starpc::Error Recv(hydra::api::ApplyBucketConfigResponse* msg) {
    return strm_->MsgRecv(msg);
  }

  starpc::Error CloseSend() { return strm_->CloseSend(); }
  starpc::Error Close() { return strm_->Close(); }

 private:
  std::unique_ptr<starpc::Stream> strm_;
};

// SRPCHydraDaemonService_ApplyBucketConfigStream is the server stream for ApplyBucketConfig.
class SRPCHydraDaemonService_ApplyBucketConfigStream {
 public:
  explicit SRPCHydraDaemonService_ApplyBucketConfigStream(starpc::Stream* strm) : strm_(strm) {}

  starpc::Error Send(const hydra::api::ApplyBucketConfigResponse& msg) {
    return strm_->MsgSend(msg);
  }

  starpc::Error SendAndClose(const hydra::api::ApplyBucketConfigResponse& msg) {
    starpc::Error err = strm_->MsgSend(msg);
    if (err != starpc::Error::OK) return err;
    return strm_->CloseSend();
  }

 private:
  starpc::Stream* strm_;
};

}  // namespace hydra::api
