// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.49.7
// source: github.com/s4wave/spacewave/db/coord/coord.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for ParticipantService.
pub const PARTICIPANT_SERVICE_SERVICE_ID: &str = "coord.ParticipantService";

/// Client trait for ParticipantService.
#[starpc::async_trait]
pub trait ParticipantServiceClient: Send + Sync {
    /// GetParticipantInfo.
    async fn get_participant_info(&self, request: &GetParticipantInfoRequest) -> starpc::Result<GetParticipantInfoResponse>;
}

/// Client implementation for ParticipantService.
pub struct ParticipantServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> ParticipantServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> ParticipantServiceClient for ParticipantServiceClientImpl<C> {
    async fn get_participant_info(&self, request: &GetParticipantInfoRequest) -> starpc::Result<GetParticipantInfoResponse> {
        self.client.exec_call("coord.ParticipantService", "GetParticipantInfo", request).await
    }
}

/// Server trait for ParticipantService.
#[starpc::async_trait]
pub trait ParticipantServiceServer: Send + Sync {
    /// GetParticipantInfo.
    async fn get_participant_info(&self, request: GetParticipantInfoRequest) -> starpc::Result<GetParticipantInfoResponse>;
}

const PARTICIPANT_SERVICE_METHOD_IDS: &[&str] = &[
    "GetParticipantInfo",
];

/// Handler for ParticipantService.
pub struct ParticipantServiceHandler<S: ParticipantServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: ParticipantServiceServer + 'static> ParticipantServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: ParticipantServiceServer + 'static> starpc::Invoker for ParticipantServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "GetParticipantInfo" => {
                let request: GetParticipantInfoRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.get_participant_info(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: ParticipantServiceServer + 'static> starpc::Handler for ParticipantServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "coord.ParticipantService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        PARTICIPANT_SERVICE_METHOD_IDS
    }
}

/// Service ID for CoordinatorService.
pub const COORDINATOR_SERVICE_SERVICE_ID: &str = "coord.CoordinatorService";

/// Stream trait for CoordinatorService.WatchWorldSeqno.
#[starpc::async_trait]
pub trait CoordinatorServiceWatchWorldSeqnoStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<WatchWorldSeqnoResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for CoordinatorService.
#[starpc::async_trait]
pub trait CoordinatorServiceClient: Send + Sync {
    /// SubmitWorldOp.
    async fn submit_world_op(&self, request: &SubmitWorldOpRequest) -> starpc::Result<SubmitWorldOpResponse>;
    /// WatchWorldSeqno.
    async fn watch_world_seqno(&self, request: &WatchWorldSeqnoRequest) -> starpc::Result<Box<dyn CoordinatorServiceWatchWorldSeqnoStream>>;
}

/// Client implementation for CoordinatorService.
pub struct CoordinatorServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> CoordinatorServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> CoordinatorServiceClient for CoordinatorServiceClientImpl<C> {
    async fn submit_world_op(&self, request: &SubmitWorldOpRequest) -> starpc::Result<SubmitWorldOpResponse> {
        self.client.exec_call("coord.CoordinatorService", "SubmitWorldOp", request).await
    }
    async fn watch_world_seqno(&self, request: &WatchWorldSeqnoRequest) -> starpc::Result<Box<dyn CoordinatorServiceWatchWorldSeqnoStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("coord.CoordinatorService", "WatchWorldSeqno", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(CoordinatorServiceWatchWorldSeqnoStreamImpl { stream }))
    }
}

struct CoordinatorServiceWatchWorldSeqnoStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl CoordinatorServiceWatchWorldSeqnoStream for CoordinatorServiceWatchWorldSeqnoStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<WatchWorldSeqnoResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for CoordinatorService.
#[starpc::async_trait]
pub trait CoordinatorServiceServer: Send + Sync {
    /// SubmitWorldOp.
    async fn submit_world_op(&self, request: SubmitWorldOpRequest) -> starpc::Result<SubmitWorldOpResponse>;
    /// WatchWorldSeqno.
    async fn watch_world_seqno(&self, request: WatchWorldSeqnoRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const COORDINATOR_SERVICE_METHOD_IDS: &[&str] = &[
    "SubmitWorldOp",
    "WatchWorldSeqno",
];

/// Handler for CoordinatorService.
pub struct CoordinatorServiceHandler<S: CoordinatorServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: CoordinatorServiceServer + 'static> CoordinatorServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: CoordinatorServiceServer + 'static> starpc::Invoker for CoordinatorServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "SubmitWorldOp" => {
                let request: SubmitWorldOpRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.submit_world_op(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "WatchWorldSeqno" => {
                let request: WatchWorldSeqnoRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.watch_world_seqno(request, stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: CoordinatorServiceServer + 'static> starpc::Handler for CoordinatorServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "coord.CoordinatorService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        COORDINATOR_SERVICE_METHOD_IDS
    }
}

