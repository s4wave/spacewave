// @generated by protoc-gen-es-starpc none with parameter "target=ts,ts_nocheck=false"
// @generated from file github.com/s4wave/spacewave/db/coord/coord.proto (package coord, syntax proto3)
/* eslint-disable */

import {
  GetParticipantInfoRequest,
  GetParticipantInfoResponse,
  SubmitWorldOpRequest,
  SubmitWorldOpResponse,
  WatchWorldSeqnoRequest,
  WatchWorldSeqnoResponse,
} from './coord.pb.js'
import { MethodKind } from '@aptre/protobuf-es-lite'
import { buildDecodeMessageTransform, MessageStream, ProtoRpc } from 'starpc'

/**
 * ParticipantService is the base RPC service exposed by every participant.
 *
 * @generated from service coord.ParticipantService
 */
export const ParticipantServiceDefinition = {
  typeName: 'coord.ParticipantService',
  methods: {
    /**
     * GetParticipantInfo returns this participant's role and capabilities.
     *
     * @generated from rpc coord.ParticipantService.GetParticipantInfo
     */
    GetParticipantInfo: {
      name: 'GetParticipantInfo',
      I: GetParticipantInfoRequest,
      O: GetParticipantInfoResponse,
      kind: MethodKind.Unary,
    },
  },
} as const

/**
 * ParticipantService is the base RPC service exposed by every participant.
 *
 * @generated from service coord.ParticipantService
 */
export interface ParticipantService {
  /**
   * GetParticipantInfo returns this participant's role and capabilities.
   *
   * @generated from rpc coord.ParticipantService.GetParticipantInfo
   */
  GetParticipantInfo(
    request: GetParticipantInfoRequest,
    abortSignal?: AbortSignal,
  ): Promise<GetParticipantInfoResponse>
}

export const ParticipantServiceServiceName =
  ParticipantServiceDefinition.typeName

export class ParticipantServiceClient implements ParticipantService {
  private readonly rpc: ProtoRpc
  private readonly service: string
  constructor(rpc: ProtoRpc, opts?: { service?: string }) {
    this.service = opts?.service || ParticipantServiceServiceName
    this.rpc = rpc
    this.GetParticipantInfo = this.GetParticipantInfo.bind(this)
  }
  /**
   * GetParticipantInfo returns this participant's role and capabilities.
   *
   * @generated from rpc coord.ParticipantService.GetParticipantInfo
   */
  async GetParticipantInfo(
    request: GetParticipantInfoRequest,
    abortSignal?: AbortSignal,
  ): Promise<GetParticipantInfoResponse> {
    const requestMsg = GetParticipantInfoRequest.create(request)
    const result = await this.rpc.request(
      this.service,
      ParticipantServiceDefinition.methods.GetParticipantInfo.name,
      GetParticipantInfoRequest.toBinary(requestMsg),
      abortSignal || undefined,
    )
    return GetParticipantInfoResponse.fromBinary(result)
  }
}
/**
 * CoordinatorService is exposed only by the leader participant.
 * Followers call these RPCs to submit world operations and watch state.
 *
 * @generated from service coord.CoordinatorService
 */
export const CoordinatorServiceDefinition = {
  typeName: 'coord.CoordinatorService',
  methods: {
    /**
     * SubmitWorldOp submits a serialized world operation to the leader for
     * execution. Returns the new sequence number after commit.
     *
     * @generated from rpc coord.CoordinatorService.SubmitWorldOp
     */
    SubmitWorldOp: {
      name: 'SubmitWorldOp',
      I: SubmitWorldOpRequest,
      O: SubmitWorldOpResponse,
      kind: MethodKind.Unary,
    },
    /**
     * WatchWorldSeqno streams world sequence number updates to followers.
     *
     * @generated from rpc coord.CoordinatorService.WatchWorldSeqno
     */
    WatchWorldSeqno: {
      name: 'WatchWorldSeqno',
      I: WatchWorldSeqnoRequest,
      O: WatchWorldSeqnoResponse,
      kind: MethodKind.ServerStreaming,
    },
  },
} as const

/**
 * CoordinatorService is exposed only by the leader participant.
 * Followers call these RPCs to submit world operations and watch state.
 *
 * @generated from service coord.CoordinatorService
 */
export interface CoordinatorService {
  /**
   * SubmitWorldOp submits a serialized world operation to the leader for
   * execution. Returns the new sequence number after commit.
   *
   * @generated from rpc coord.CoordinatorService.SubmitWorldOp
   */
  SubmitWorldOp(
    request: SubmitWorldOpRequest,
    abortSignal?: AbortSignal,
  ): Promise<SubmitWorldOpResponse>

  /**
   * WatchWorldSeqno streams world sequence number updates to followers.
   *
   * @generated from rpc coord.CoordinatorService.WatchWorldSeqno
   */
  WatchWorldSeqno(
    request: WatchWorldSeqnoRequest,
    abortSignal?: AbortSignal,
  ): MessageStream<WatchWorldSeqnoResponse>
}

export const CoordinatorServiceServiceName =
  CoordinatorServiceDefinition.typeName

export class CoordinatorServiceClient implements CoordinatorService {
  private readonly rpc: ProtoRpc
  private readonly service: string
  constructor(rpc: ProtoRpc, opts?: { service?: string }) {
    this.service = opts?.service || CoordinatorServiceServiceName
    this.rpc = rpc
    this.SubmitWorldOp = this.SubmitWorldOp.bind(this)
    this.WatchWorldSeqno = this.WatchWorldSeqno.bind(this)
  }
  /**
   * SubmitWorldOp submits a serialized world operation to the leader for
   * execution. Returns the new sequence number after commit.
   *
   * @generated from rpc coord.CoordinatorService.SubmitWorldOp
   */
  async SubmitWorldOp(
    request: SubmitWorldOpRequest,
    abortSignal?: AbortSignal,
  ): Promise<SubmitWorldOpResponse> {
    const requestMsg = SubmitWorldOpRequest.create(request)
    const result = await this.rpc.request(
      this.service,
      CoordinatorServiceDefinition.methods.SubmitWorldOp.name,
      SubmitWorldOpRequest.toBinary(requestMsg),
      abortSignal || undefined,
    )
    return SubmitWorldOpResponse.fromBinary(result)
  }

  /**
   * WatchWorldSeqno streams world sequence number updates to followers.
   *
   * @generated from rpc coord.CoordinatorService.WatchWorldSeqno
   */
  WatchWorldSeqno(
    request: WatchWorldSeqnoRequest,
    abortSignal?: AbortSignal,
  ): MessageStream<WatchWorldSeqnoResponse> {
    const requestMsg = WatchWorldSeqnoRequest.create(request)
    const result = this.rpc.serverStreamingRequest(
      this.service,
      CoordinatorServiceDefinition.methods.WatchWorldSeqno.name,
      WatchWorldSeqnoRequest.toBinary(requestMsg),
      abortSignal || undefined,
    )
    return buildDecodeMessageTransform(WatchWorldSeqnoResponse)(result)
  }
}
