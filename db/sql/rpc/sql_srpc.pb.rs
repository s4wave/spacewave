// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.49.18
// source: github.com/s4wave/spacewave/db/sql/rpc/sql.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for Sql.
pub const SQL_SERVICE_ID: &str = "sql.rpc.Sql";

/// Stream trait for Sql.SqlTransaction.
#[starpc::async_trait]
pub trait SqlSqlTransactionStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &SqlTransactionRequest) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<SqlTransactionResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Stream trait for Sql.SqlTransactionRpc.
#[starpc::async_trait]
pub trait SqlSqlTransactionRpcStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<RpcStreamPacket>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for Sql.
#[starpc::async_trait]
pub trait SqlClient: Send + Sync {
    /// SqlTransaction.
    async fn sql_transaction(&self) -> starpc::Result<Box<dyn SqlSqlTransactionStream>>;
    /// SqlTransactionRpc.
    async fn sql_transaction_rpc(&self) -> starpc::Result<Box<dyn SqlSqlTransactionRpcStream>>;
}

/// Client implementation for Sql.
pub struct SqlClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> SqlClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> SqlClient for SqlClientImpl<C> {
    async fn sql_transaction(&self) -> starpc::Result<Box<dyn SqlSqlTransactionStream>> {
        let stream = self.client.new_stream("sql.rpc.Sql", "SqlTransaction", None).await?;
        Ok(Box::new(SqlSqlTransactionStreamImpl { stream }))
    }
    async fn sql_transaction_rpc(&self) -> starpc::Result<Box<dyn SqlSqlTransactionRpcStream>> {
        let stream = self.client.new_stream("sql.rpc.Sql", "SqlTransactionRpc", None).await?;
        Ok(Box::new(SqlSqlTransactionRpcStreamImpl { stream }))
    }
}

struct SqlSqlTransactionStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl SqlSqlTransactionStream for SqlSqlTransactionStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &SqlTransactionRequest) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<SqlTransactionResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

struct SqlSqlTransactionRpcStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl SqlSqlTransactionRpcStream for SqlSqlTransactionRpcStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<RpcStreamPacket> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for Sql.
#[starpc::async_trait]
pub trait SqlServer: Send + Sync {
    /// SqlTransaction.
    async fn sql_transaction(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// SqlTransactionRpc.
    async fn sql_transaction_rpc(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const SQL_METHOD_IDS: &[&str] = &[
    "SqlTransaction",
    "SqlTransactionRpc",
];

/// Handler for Sql.
pub struct SqlHandler<S: SqlServer> {
    server: std::sync::Arc<S>,
}

impl<S: SqlServer + 'static> SqlHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: SqlServer + 'static> starpc::Invoker for SqlHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "SqlTransaction" => {
                (true, self.server.sql_transaction(stream).await)
            }
            "SqlTransactionRpc" => {
                (true, self.server.sql_transaction_rpc(stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: SqlServer + 'static> starpc::Handler for SqlHandler<S> {
    fn service_id(&self) -> &'static str {
        "sql.rpc.Sql"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        SQL_METHOD_IDS
    }
}

/// Service ID for SqlOps.
pub const SQL_OPS_SERVICE_ID: &str = "sql.rpc.SqlOps";

/// Stream trait for SqlOps.Query.
#[starpc::async_trait]
pub trait SqlOpsQueryStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &SqlQueryRequest) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<SqlQueryResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for SqlOps.
#[starpc::async_trait]
pub trait SqlOpsClient: Send + Sync {
    /// Exec.
    async fn exec(&self, request: &SqlExecRequest) -> starpc::Result<SqlExecResponse>;
    /// Query.
    async fn query(&self) -> starpc::Result<Box<dyn SqlOpsQueryStream>>;
}

/// Client implementation for SqlOps.
pub struct SqlOpsClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> SqlOpsClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> SqlOpsClient for SqlOpsClientImpl<C> {
    async fn exec(&self, request: &SqlExecRequest) -> starpc::Result<SqlExecResponse> {
        self.client.exec_call("sql.rpc.SqlOps", "Exec", request).await
    }
    async fn query(&self) -> starpc::Result<Box<dyn SqlOpsQueryStream>> {
        let stream = self.client.new_stream("sql.rpc.SqlOps", "Query", None).await?;
        Ok(Box::new(SqlOpsQueryStreamImpl { stream }))
    }
}

struct SqlOpsQueryStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl SqlOpsQueryStream for SqlOpsQueryStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &SqlQueryRequest) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<SqlQueryResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for SqlOps.
#[starpc::async_trait]
pub trait SqlOpsServer: Send + Sync {
    /// Exec.
    async fn exec(&self, request: SqlExecRequest) -> starpc::Result<SqlExecResponse>;
    /// Query.
    async fn query(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const SQL_OPS_METHOD_IDS: &[&str] = &[
    "Exec",
    "Query",
];

/// Handler for SqlOps.
pub struct SqlOpsHandler<S: SqlOpsServer> {
    server: std::sync::Arc<S>,
}

impl<S: SqlOpsServer + 'static> SqlOpsHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: SqlOpsServer + 'static> starpc::Invoker for SqlOpsHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "Exec" => {
                let request: SqlExecRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.exec(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "Query" => {
                (true, self.server.query(stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: SqlOpsServer + 'static> starpc::Handler for SqlOpsHandler<S> {
    fn service_id(&self) -> &'static str {
        "sql.rpc.SqlOps"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        SQL_OPS_METHOD_IDS
    }
}

