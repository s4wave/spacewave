// @generated by protoc-gen-es-starpc none with parameter "target=ts,ts_nocheck=false"
// @generated from file github.com/s4wave/spacewave/db/unixfs/v86fs/v86fs.proto (package unixfs.v86fs, syntax proto3)
/* eslint-disable */

import { V86fsMessage } from './v86fs.pb.js'
import { MethodKind } from '@aptre/protobuf-es-lite'
import {
  buildDecodeMessageTransform,
  buildEncodeMessageTransform,
  MessageStream,
  ProtoRpc,
} from 'starpc'

/**
 * V86fsService relays v86fs operations between a browser VM and FSHandle storage.
 * Both client and server send V86fsMessage on the stream.
 * Client sends requests, server sends replies and push notifications.
 *
 * @generated from service unixfs.v86fs.V86fsService
 */
export const V86fsServiceDefinition = {
  typeName: 'unixfs.v86fs.V86fsService',
  methods: {
    /**
     * RelayV86fs opens a bidirectional stream for v86fs message relay.
     * The client sends V86fsMessage requests, the server sends replies
     * and push notifications (invalidation, mount/umount).
     *
     * @generated from rpc unixfs.v86fs.V86fsService.RelayV86fs
     */
    RelayV86fs: {
      name: 'RelayV86fs',
      I: V86fsMessage,
      O: V86fsMessage,
      kind: MethodKind.BiDiStreaming,
    },
  },
} as const

/**
 * V86fsService relays v86fs operations between a browser VM and FSHandle storage.
 * Both client and server send V86fsMessage on the stream.
 * Client sends requests, server sends replies and push notifications.
 *
 * @generated from service unixfs.v86fs.V86fsService
 */
export interface V86fsService {
  /**
   * RelayV86fs opens a bidirectional stream for v86fs message relay.
   * The client sends V86fsMessage requests, the server sends replies
   * and push notifications (invalidation, mount/umount).
   *
   * @generated from rpc unixfs.v86fs.V86fsService.RelayV86fs
   */
  RelayV86fs(
    request: MessageStream<V86fsMessage>,
    abortSignal?: AbortSignal,
  ): MessageStream<V86fsMessage>
}

export const V86fsServiceServiceName = V86fsServiceDefinition.typeName

export class V86fsServiceClient implements V86fsService {
  private readonly rpc: ProtoRpc
  private readonly service: string
  constructor(rpc: ProtoRpc, opts?: { service?: string }) {
    this.service = opts?.service || V86fsServiceServiceName
    this.rpc = rpc
    this.RelayV86fs = this.RelayV86fs.bind(this)
  }
  /**
   * RelayV86fs opens a bidirectional stream for v86fs message relay.
   * The client sends V86fsMessage requests, the server sends replies
   * and push notifications (invalidation, mount/umount).
   *
   * @generated from rpc unixfs.v86fs.V86fsService.RelayV86fs
   */
  RelayV86fs(
    request: MessageStream<V86fsMessage>,
    abortSignal?: AbortSignal,
  ): MessageStream<V86fsMessage> {
    const result = this.rpc.bidirectionalStreamingRequest(
      this.service,
      V86fsServiceDefinition.methods.RelayV86fs.name,
      buildEncodeMessageTransform(V86fsMessage)(request),
      abortSignal || undefined,
    )
    return buildDecodeMessageTransform(V86fsMessage)(result)
  }
}
