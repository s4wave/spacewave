# Run Execution

This is a simple example of loading a yaml Target and running a one-off
Execution controller.

```sh
./run-execution ../targets/02-kvtx.yaml
```

The 02-kvtx example will perform a variety of operations on a block-graph backed
key value store (usually a kv tree).
