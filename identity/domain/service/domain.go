package identity_domain_service

import "github.com/s4wave/spacewave/net/protocol"

// IdentityDomainProtocol is the identity domain lookup service protocol.
const IdentityDomainProtocol = protocol.ID("identity/domain")
