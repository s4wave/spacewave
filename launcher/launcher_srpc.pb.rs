// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.47.1
// source: github.com/aperturerobotics/bldr/launcher/launcher.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for Launcher.
pub const LAUNCHER_SERVICE_ID: &str = "bldr.launcher.Launcher";

/// Stream trait for Launcher.WatchLauncherInfo.
#[starpc::async_trait]
pub trait LauncherWatchLauncherInfoStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<LauncherInfo>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for Launcher.
#[starpc::async_trait]
pub trait LauncherClient: Send + Sync {
    /// WatchLauncherInfo.
    async fn watch_launcher_info(&self, request: &WatchLauncherInfoRequest) -> starpc::Result<Box<dyn LauncherWatchLauncherInfoStream>>;
    /// PushDistConfigMsg.
    async fn push_dist_config_msg(&self, request: &PushDistConfigRequest) -> starpc::Result<PushDistConfigResponse>;
    /// RecheckDistConfig.
    async fn recheck_dist_config(&self, request: &RecheckDistConfigRequest) -> starpc::Result<RecheckDistConfigResponse>;
}

/// Client implementation for Launcher.
pub struct LauncherClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> LauncherClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> LauncherClient for LauncherClientImpl<C> {
    async fn watch_launcher_info(&self, request: &WatchLauncherInfoRequest) -> starpc::Result<Box<dyn LauncherWatchLauncherInfoStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("bldr.launcher.Launcher", "WatchLauncherInfo", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(LauncherWatchLauncherInfoStreamImpl { stream }))
    }
    async fn push_dist_config_msg(&self, request: &PushDistConfigRequest) -> starpc::Result<PushDistConfigResponse> {
        self.client.exec_call("bldr.launcher.Launcher", "PushDistConfigMsg", request).await
    }
    async fn recheck_dist_config(&self, request: &RecheckDistConfigRequest) -> starpc::Result<RecheckDistConfigResponse> {
        self.client.exec_call("bldr.launcher.Launcher", "RecheckDistConfig", request).await
    }
}

struct LauncherWatchLauncherInfoStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl LauncherWatchLauncherInfoStream for LauncherWatchLauncherInfoStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<LauncherInfo> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for Launcher.
#[starpc::async_trait]
pub trait LauncherServer: Send + Sync {
    /// WatchLauncherInfo.
    async fn watch_launcher_info(&self, request: WatchLauncherInfoRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// PushDistConfigMsg.
    async fn push_dist_config_msg(&self, request: PushDistConfigRequest) -> starpc::Result<PushDistConfigResponse>;
    /// RecheckDistConfig.
    async fn recheck_dist_config(&self, request: RecheckDistConfigRequest) -> starpc::Result<RecheckDistConfigResponse>;
}

const LAUNCHER_METHOD_IDS: &[&str] = &[
    "WatchLauncherInfo",
    "PushDistConfigMsg",
    "RecheckDistConfig",
];

/// Handler for Launcher.
pub struct LauncherHandler<S: LauncherServer> {
    server: std::sync::Arc<S>,
}

impl<S: LauncherServer + 'static> LauncherHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: LauncherServer + 'static> starpc::Invoker for LauncherHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "WatchLauncherInfo" => {
                let request: WatchLauncherInfoRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.watch_launcher_info(request, stream).await)
            }
            "PushDistConfigMsg" => {
                let request: PushDistConfigRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.push_dist_config_msg(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "RecheckDistConfig" => {
                let request: RecheckDistConfigRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.recheck_dist_config(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: LauncherServer + 'static> starpc::Handler for LauncherHandler<S> {
    fn service_id(&self) -> &'static str {
        "bldr.launcher.Launcher"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        LAUNCHER_METHOD_IDS
    }
}

