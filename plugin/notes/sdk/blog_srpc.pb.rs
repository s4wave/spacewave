// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.49.5
// source: github.com/s4wave/spacewave/plugin/notes/sdk/blog.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for BlogResourceService.
pub const BLOG_RESOURCE_SERVICE_SERVICE_ID: &str = "notes.BlogResourceService";

/// Stream trait for BlogResourceService.WatchBlog.
#[starpc::async_trait]
pub trait BlogResourceServiceWatchBlogStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<WatchBlogResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for BlogResourceService.
#[starpc::async_trait]
pub trait BlogResourceServiceClient: Send + Sync {
    /// WatchBlog.
    async fn watch_blog(&self, request: &WatchBlogRequest) -> starpc::Result<Box<dyn BlogResourceServiceWatchBlogStream>>;
}

/// Client implementation for BlogResourceService.
pub struct BlogResourceServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> BlogResourceServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> BlogResourceServiceClient for BlogResourceServiceClientImpl<C> {
    async fn watch_blog(&self, request: &WatchBlogRequest) -> starpc::Result<Box<dyn BlogResourceServiceWatchBlogStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("notes.BlogResourceService", "WatchBlog", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(BlogResourceServiceWatchBlogStreamImpl { stream }))
    }
}

struct BlogResourceServiceWatchBlogStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl BlogResourceServiceWatchBlogStream for BlogResourceServiceWatchBlogStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<WatchBlogResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for BlogResourceService.
#[starpc::async_trait]
pub trait BlogResourceServiceServer: Send + Sync {
    /// WatchBlog.
    async fn watch_blog(&self, request: WatchBlogRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const BLOG_RESOURCE_SERVICE_METHOD_IDS: &[&str] = &[
    "WatchBlog",
];

/// Handler for BlogResourceService.
pub struct BlogResourceServiceHandler<S: BlogResourceServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: BlogResourceServiceServer + 'static> BlogResourceServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: BlogResourceServiceServer + 'static> starpc::Invoker for BlogResourceServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "WatchBlog" => {
                let request: WatchBlogRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.watch_blog(request, stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: BlogResourceServiceServer + 'static> starpc::Handler for BlogResourceServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "notes.BlogResourceService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        BLOG_RESOURCE_SERVICE_METHOD_IDS
    }
}

