//go:build deps_only && cgo

// Code generated by protoc-gen-starpc-cpp. DO NOT EDIT.
// protoc-gen-starpc-cpp version: v0.49.5
// source: github.com/s4wave/spacewave/plugin/notes/sdk/docs.proto

#pragma once

#include <functional>
#include <memory>
#include <string>
#include <utility>

#include "srpc/starpc.hpp"

#include "docs.pb.h"

namespace notes {

// Service ID for DocsResourceService
constexpr const char* kSRPCDocsResourceServiceServiceID = "notes.DocsResourceService";

class SRPCDocsResourceService_WatchDocsClient;
class SRPCDocsResourceService_WatchDocsStream;

// SRPCDocsResourceServiceClient is the client API for DocsResourceService service.
class SRPCDocsResourceServiceClient {
 public:
  virtual ~SRPCDocsResourceServiceClient() = default;

  // SRPCClient returns the underlying SRPC client.
  virtual starpc::Client* SRPCClient() = 0;

  // WatchDocs
  virtual std::pair<std::unique_ptr<SRPCDocsResourceService_WatchDocsClient>, starpc::Error> WatchDocs(const notes::WatchDocsRequest& in) = 0;
};

// SRPCDocsResourceServiceClientImpl implements SRPCDocsResourceServiceClient.
class SRPCDocsResourceServiceClientImpl : public SRPCDocsResourceServiceClient {
 public:
  explicit SRPCDocsResourceServiceClientImpl(starpc::Client* cc, const std::string& service_id = "")
      : cc_(cc), service_id_(service_id.empty() ? kSRPCDocsResourceServiceServiceID : service_id) {}

  starpc::Client* SRPCClient() override { return cc_; }

  // WatchDocs
  virtual std::pair<std::unique_ptr<SRPCDocsResourceService_WatchDocsClient>, starpc::Error> WatchDocs(const notes::WatchDocsRequest& in) override;

 private:
  starpc::Client* cc_;
  std::string service_id_;
};

// NewSRPCDocsResourceServiceClient creates a new client.
inline std::unique_ptr<SRPCDocsResourceServiceClient> NewSRPCDocsResourceServiceClient(starpc::Client* cc) {
  return std::make_unique<SRPCDocsResourceServiceClientImpl>(cc);
}

// SRPCDocsResourceServiceServer is the server API for DocsResourceService service.
class SRPCDocsResourceServiceServer {
 public:
  virtual ~SRPCDocsResourceServiceServer() = default;

  // WatchDocs
  virtual starpc::Error WatchDocs(const notes::WatchDocsRequest& req, SRPCDocsResourceService_WatchDocsStream* strm) = 0;
};

// SRPCDocsResourceServiceHandler implements starpc::Handler for DocsResourceService.
class SRPCDocsResourceServiceHandler : public starpc::Handler {
 public:
  SRPCDocsResourceServiceHandler(SRPCDocsResourceServiceServer* impl, const std::string& service_id = "")
      : impl_(impl), service_id_(service_id.empty() ? kSRPCDocsResourceServiceServiceID : service_id) {}

  const std::string& GetServiceID() const override { return service_id_; }
  std::vector<std::string> GetMethodIDs() const override;
  std::pair<bool, starpc::Error> InvokeMethod(
      const std::string& service_id,
      const std::string& method_id,
      starpc::Stream* strm) override;

 private:
  SRPCDocsResourceServiceServer* impl_;
  std::string service_id_;
};

// NewSRPCDocsResourceServiceHandler creates a new handler for the given implementation.
inline std::unique_ptr<SRPCDocsResourceServiceHandler> NewSRPCDocsResourceServiceHandler(SRPCDocsResourceServiceServer* impl) {
  return std::make_unique<SRPCDocsResourceServiceHandler>(impl);
}

// SRPCRegisterDocsResourceService registers the server implementation with the mux.
// The returned handler must outlive the mux registration.
inline std::pair<std::unique_ptr<SRPCDocsResourceServiceHandler>, starpc::Error> SRPCRegisterDocsResourceService(starpc::Mux* mux, SRPCDocsResourceServiceServer* impl) {
  auto handler = NewSRPCDocsResourceServiceHandler(impl);
  starpc::Error err = mux->Register(handler.get());
  if (err != starpc::Error::OK) {
    return {nullptr, err};
  }
  return {std::move(handler), starpc::Error::OK};
}

// SRPCDocsResourceService_WatchDocsClient is the client stream for WatchDocs.
class SRPCDocsResourceService_WatchDocsClient {
 public:
  explicit SRPCDocsResourceService_WatchDocsClient(std::unique_ptr<starpc::Stream> strm) : strm_(std::move(strm)) {}

  starpc::Error Recv(notes::WatchDocsResponse* msg) {
    return strm_->MsgRecv(msg);
  }

  starpc::Error CloseSend() { return strm_->CloseSend(); }
  starpc::Error Close() { return strm_->Close(); }

 private:
  std::unique_ptr<starpc::Stream> strm_;
};

// SRPCDocsResourceService_WatchDocsStream is the server stream for WatchDocs.
class SRPCDocsResourceService_WatchDocsStream {
 public:
  explicit SRPCDocsResourceService_WatchDocsStream(starpc::Stream* strm) : strm_(strm) {}

  starpc::Error Send(const notes::WatchDocsResponse& msg) {
    return strm_->MsgSend(msg);
  }

  starpc::Error SendAndClose(const notes::WatchDocsResponse& msg) {
    starpc::Error err = strm_->MsgSend(msg);
    if (err != starpc::Error::OK) return err;
    return strm_->CloseSend();
  }

 private:
  starpc::Stream* strm_;
};

}  // namespace notes
