// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.49.5
// source: github.com/s4wave/spacewave/plugin/notes/sdk/docs.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for DocsResourceService.
pub const DOCS_RESOURCE_SERVICE_SERVICE_ID: &str = "notes.DocsResourceService";

/// Stream trait for DocsResourceService.WatchDocs.
#[starpc::async_trait]
pub trait DocsResourceServiceWatchDocsStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<WatchDocsResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for DocsResourceService.
#[starpc::async_trait]
pub trait DocsResourceServiceClient: Send + Sync {
    /// WatchDocs.
    async fn watch_docs(&self, request: &WatchDocsRequest) -> starpc::Result<Box<dyn DocsResourceServiceWatchDocsStream>>;
}

/// Client implementation for DocsResourceService.
pub struct DocsResourceServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> DocsResourceServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> DocsResourceServiceClient for DocsResourceServiceClientImpl<C> {
    async fn watch_docs(&self, request: &WatchDocsRequest) -> starpc::Result<Box<dyn DocsResourceServiceWatchDocsStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("notes.DocsResourceService", "WatchDocs", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(DocsResourceServiceWatchDocsStreamImpl { stream }))
    }
}

struct DocsResourceServiceWatchDocsStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl DocsResourceServiceWatchDocsStream for DocsResourceServiceWatchDocsStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<WatchDocsResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for DocsResourceService.
#[starpc::async_trait]
pub trait DocsResourceServiceServer: Send + Sync {
    /// WatchDocs.
    async fn watch_docs(&self, request: WatchDocsRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const DOCS_RESOURCE_SERVICE_METHOD_IDS: &[&str] = &[
    "WatchDocs",
];

/// Handler for DocsResourceService.
pub struct DocsResourceServiceHandler<S: DocsResourceServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: DocsResourceServiceServer + 'static> DocsResourceServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: DocsResourceServiceServer + 'static> starpc::Invoker for DocsResourceServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "WatchDocs" => {
                let request: WatchDocsRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.watch_docs(request, stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: DocsResourceServiceServer + 'static> starpc::Handler for DocsResourceServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "notes.DocsResourceService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        DOCS_RESOURCE_SERVICE_METHOD_IDS
    }
}

