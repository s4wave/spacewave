// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.45.1-0.20260203090429-3e915608d4e8
// source: github.com/aperturerobotics/bldr/prototypes/webworker-rpcstream/common/common.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for PrototypeService.
pub const PROTOTYPE_SERVICE_SERVICE_ID: &str = "prototype.webworker_rpcstream.common.PrototypeService";

/// Stream trait for PrototypeService.Prototype.
#[starpc::async_trait]
pub trait PrototypeServicePrototypeStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<PrototypeResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for PrototypeService.
#[starpc::async_trait]
pub trait PrototypeServiceClient: Send + Sync {
    /// Prototype.
    async fn prototype(&self, request: &PrototypeRequest) -> starpc::Result<Box<dyn PrototypeServicePrototypeStream>>;
}

/// Client implementation for PrototypeService.
pub struct PrototypeServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> PrototypeServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> PrototypeServiceClient for PrototypeServiceClientImpl<C> {
    async fn prototype(&self, request: &PrototypeRequest) -> starpc::Result<Box<dyn PrototypeServicePrototypeStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("prototype.webworker_rpcstream.common.PrototypeService", "Prototype", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(PrototypeServicePrototypeStreamImpl { stream }))
    }
}

struct PrototypeServicePrototypeStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl PrototypeServicePrototypeStream for PrototypeServicePrototypeStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<PrototypeResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for PrototypeService.
#[starpc::async_trait]
pub trait PrototypeServiceServer: Send + Sync {
    /// Prototype.
    async fn prototype(&self, request: PrototypeRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const PROTOTYPE_SERVICE_METHOD_IDS: &[&str] = &[
    "Prototype",
];

/// Handler for PrototypeService.
pub struct PrototypeServiceHandler<S: PrototypeServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: PrototypeServiceServer + 'static> PrototypeServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: PrototypeServiceServer + 'static> starpc::Invoker for PrototypeServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "Prototype" => {
                let request: PrototypeRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.prototype(request, stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: PrototypeServiceServer + 'static> starpc::Handler for PrototypeServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "prototype.webworker_rpcstream.common.PrototypeService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        PROTOTYPE_SERVICE_METHOD_IDS
    }
}

