// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.47.1
// source: github.com/aperturerobotics/bldr/resource/resource.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for ResourceService.
pub const RESOURCE_SERVICE_SERVICE_ID: &str = "resource.ResourceService";

/// Stream trait for ResourceService.ResourceClient.
#[starpc::async_trait]
pub trait ResourceServiceResourceClientStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<ResourceClientResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Stream trait for ResourceService.ResourceRpc.
#[starpc::async_trait]
pub trait ResourceServiceResourceRpcStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<RpcStreamPacket>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for ResourceService.
#[starpc::async_trait]
pub trait ResourceServiceClient: Send + Sync {
    /// ResourceClient.
    async fn resource_client(&self, request: &ResourceClientRequest) -> starpc::Result<Box<dyn ResourceServiceResourceClientStream>>;
    /// ResourceRpc.
    async fn resource_rpc(&self) -> starpc::Result<Box<dyn ResourceServiceResourceRpcStream>>;
    /// ResourceRefRelease.
    async fn resource_ref_release(&self, request: &ResourceRefReleaseRequest) -> starpc::Result<ResourceRefReleaseResponse>;
}

/// Client implementation for ResourceService.
pub struct ResourceServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> ResourceServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> ResourceServiceClient for ResourceServiceClientImpl<C> {
    async fn resource_client(&self, request: &ResourceClientRequest) -> starpc::Result<Box<dyn ResourceServiceResourceClientStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("resource.ResourceService", "ResourceClient", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(ResourceServiceResourceClientStreamImpl { stream }))
    }
    async fn resource_rpc(&self) -> starpc::Result<Box<dyn ResourceServiceResourceRpcStream>> {
        let stream = self.client.new_stream("resource.ResourceService", "ResourceRpc", None).await?;
        Ok(Box::new(ResourceServiceResourceRpcStreamImpl { stream }))
    }
    async fn resource_ref_release(&self, request: &ResourceRefReleaseRequest) -> starpc::Result<ResourceRefReleaseResponse> {
        self.client.exec_call("resource.ResourceService", "ResourceRefRelease", request).await
    }
}

struct ResourceServiceResourceClientStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl ResourceServiceResourceClientStream for ResourceServiceResourceClientStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<ResourceClientResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

struct ResourceServiceResourceRpcStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl ResourceServiceResourceRpcStream for ResourceServiceResourceRpcStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<RpcStreamPacket> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for ResourceService.
#[starpc::async_trait]
pub trait ResourceServiceServer: Send + Sync {
    /// ResourceClient.
    async fn resource_client(&self, request: ResourceClientRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// ResourceRpc.
    async fn resource_rpc(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// ResourceRefRelease.
    async fn resource_ref_release(&self, request: ResourceRefReleaseRequest) -> starpc::Result<ResourceRefReleaseResponse>;
}

const RESOURCE_SERVICE_METHOD_IDS: &[&str] = &[
    "ResourceClient",
    "ResourceRpc",
    "ResourceRefRelease",
];

/// Handler for ResourceService.
pub struct ResourceServiceHandler<S: ResourceServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: ResourceServiceServer + 'static> ResourceServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: ResourceServiceServer + 'static> starpc::Invoker for ResourceServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "ResourceClient" => {
                let request: ResourceClientRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.resource_client(request, stream).await)
            }
            "ResourceRpc" => {
                (true, self.server.resource_rpc(stream).await)
            }
            "ResourceRefRelease" => {
                let request: ResourceRefReleaseRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.resource_ref_release(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: ResourceServiceServer + 'static> starpc::Handler for ResourceServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "resource.ResourceService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        RESOURCE_SERVICE_METHOD_IDS
    }
}

