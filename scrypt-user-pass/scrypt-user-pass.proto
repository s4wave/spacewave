syntax = "proto3";
package scryptuserpass;

// AuthCredentials are the auth credentials for ScryptUserPass.
message AuthCredentials {
  // Username is the user's username.
  string username = 1;
  // Password is the user's plaintext password.
  string password = 2;
}
