// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.46.1
// source: github.com/s4wave/spacewave/sdk/deploy/deploy.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for DeployService.
pub const DEPLOY_SERVICE_SERVICE_ID: &str = "s4wave.deploy.DeployService";

/// Stream trait for DeployService.DeployManifest.
#[starpc::async_trait]
pub trait DeployServiceDeployManifestStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &DeployManifestMessage) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<DeployManifestMessage>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for DeployService.
#[starpc::async_trait]
pub trait DeployServiceClient: Send + Sync {
    /// DeployManifest.
    async fn deploy_manifest(&self) -> starpc::Result<Box<dyn DeployServiceDeployManifestStream>>;
}

/// Client implementation for DeployService.
pub struct DeployServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> DeployServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> DeployServiceClient for DeployServiceClientImpl<C> {
    async fn deploy_manifest(&self) -> starpc::Result<Box<dyn DeployServiceDeployManifestStream>> {
        let stream = self.client.new_stream("s4wave.deploy.DeployService", "DeployManifest", None).await?;
        Ok(Box::new(DeployServiceDeployManifestStreamImpl { stream }))
    }
}

struct DeployServiceDeployManifestStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl DeployServiceDeployManifestStream for DeployServiceDeployManifestStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &DeployManifestMessage) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<DeployManifestMessage> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for DeployService.
#[starpc::async_trait]
pub trait DeployServiceServer: Send + Sync {
    /// DeployManifest.
    async fn deploy_manifest(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const DEPLOY_SERVICE_METHOD_IDS: &[&str] = &[
    "DeployManifest",
];

/// Handler for DeployService.
pub struct DeployServiceHandler<S: DeployServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: DeployServiceServer + 'static> DeployServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: DeployServiceServer + 'static> starpc::Invoker for DeployServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "DeployManifest" => {
                (true, self.server.deploy_manifest(stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: DeployServiceServer + 'static> starpc::Handler for DeployServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "s4wave.deploy.DeployService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        DEPLOY_SERVICE_METHOD_IDS
    }
}

