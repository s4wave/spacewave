// @generated by protoc-gen-es-starpc none with parameter "target=ts,ts_nocheck=false"
// @generated from file github.com/s4wave/spacewave/sdk/deploy/deploy.proto (package s4wave.deploy, syntax proto3)
/* eslint-disable */

import { DeployManifestMessage } from './deploy.pb.js'
import { MethodKind } from '@aptre/protobuf-es-lite'
import {
  buildDecodeMessageTransform,
  buildEncodeMessageTransform,
  MessageStream,
  ProtoRpc,
} from 'starpc'

/**
 * DeployService provides manifest deployment to Spaces.
 *
 * @generated from service s4wave.deploy.DeployService
 */
export const DeployServiceDefinition = {
  typeName: 's4wave.deploy.DeployService',
  methods: {
    /**
     * DeployManifest deploys a manifest and its blocks into a Space.
     * Bidirectional stream: client serves blocks, server requests them.
     *
     * @generated from rpc s4wave.deploy.DeployService.DeployManifest
     */
    DeployManifest: {
      name: 'DeployManifest',
      I: DeployManifestMessage,
      O: DeployManifestMessage,
      kind: MethodKind.BiDiStreaming,
    },
  },
} as const

/**
 * DeployService provides manifest deployment to Spaces.
 *
 * @generated from service s4wave.deploy.DeployService
 */
export interface DeployService {
  /**
   * DeployManifest deploys a manifest and its blocks into a Space.
   * Bidirectional stream: client serves blocks, server requests them.
   *
   * @generated from rpc s4wave.deploy.DeployService.DeployManifest
   */
  DeployManifest(
    request: MessageStream<DeployManifestMessage>,
    abortSignal?: AbortSignal,
  ): MessageStream<DeployManifestMessage>
}

export const DeployServiceServiceName = DeployServiceDefinition.typeName

export class DeployServiceClient implements DeployService {
  private readonly rpc: ProtoRpc
  private readonly service: string
  constructor(rpc: ProtoRpc, opts?: { service?: string }) {
    this.service = opts?.service || DeployServiceServiceName
    this.rpc = rpc
    this.DeployManifest = this.DeployManifest.bind(this)
  }
  /**
   * DeployManifest deploys a manifest and its blocks into a Space.
   * Bidirectional stream: client serves blocks, server requests them.
   *
   * @generated from rpc s4wave.deploy.DeployService.DeployManifest
   */
  DeployManifest(
    request: MessageStream<DeployManifestMessage>,
    abortSignal?: AbortSignal,
  ): MessageStream<DeployManifestMessage> {
    const result = this.rpc.bidirectionalStreamingRequest(
      this.service,
      DeployServiceDefinition.methods.DeployManifest.name,
      buildEncodeMessageTransform(DeployManifestMessage)(request),
      abortSignal || undefined,
    )
    return buildDecodeMessageTransform(DeployManifestMessage)(result)
  }
}
