//go:build deps_only && cgo

// Code generated by protoc-gen-starpc-cpp. DO NOT EDIT.
// protoc-gen-starpc-cpp version: v0.49.7
// source: github.com/s4wave/spacewave/sdk/git/worktree.proto

#pragma once

#include <functional>
#include <memory>
#include <string>
#include <utility>

#include "srpc/starpc.hpp"

#include "worktree.pb.h"

namespace s4wave::git {

// Service ID for GitWorktreeResourceService
constexpr const char* kSRPCGitWorktreeResourceServiceServiceID = "s4wave.git.GitWorktreeResourceService";

class SRPCGitWorktreeResourceService_WatchStatusClient;
class SRPCGitWorktreeResourceService_WatchStatusStream;

// SRPCGitWorktreeResourceServiceClient is the client API for GitWorktreeResourceService service.
class SRPCGitWorktreeResourceServiceClient {
 public:
  virtual ~SRPCGitWorktreeResourceServiceClient() = default;

  // SRPCClient returns the underlying SRPC client.
  virtual starpc::Client* SRPCClient() = 0;

  // GetWorktreeInfo
  virtual starpc::Error GetWorktreeInfo(const s4wave::git::GetWorktreeInfoRequest& in, s4wave::git::GetWorktreeInfoResponse* out) = 0;
  // GetRepoResource
  virtual starpc::Error GetRepoResource(const s4wave::git::GetRepoResourceRequest& in, s4wave::git::GetRepoResourceResponse* out) = 0;
  // GetWorkdirResource
  virtual starpc::Error GetWorkdirResource(const s4wave::git::GetWorkdirResourceRequest& in, s4wave::git::GetWorkdirResourceResponse* out) = 0;
  // WatchStatus
  virtual std::pair<std::unique_ptr<SRPCGitWorktreeResourceService_WatchStatusClient>, starpc::Error> WatchStatus(const s4wave::git::WatchStatusRequest& in) = 0;
  // StageFiles
  virtual starpc::Error StageFiles(const s4wave::git::StageFilesRequest& in, s4wave::git::StageFilesResponse* out) = 0;
  // UnstageFiles
  virtual starpc::Error UnstageFiles(const s4wave::git::UnstageFilesRequest& in, s4wave::git::UnstageFilesResponse* out) = 0;
};

// SRPCGitWorktreeResourceServiceClientImpl implements SRPCGitWorktreeResourceServiceClient.
class SRPCGitWorktreeResourceServiceClientImpl : public SRPCGitWorktreeResourceServiceClient {
 public:
  explicit SRPCGitWorktreeResourceServiceClientImpl(starpc::Client* cc, const std::string& service_id = "")
      : cc_(cc), service_id_(service_id.empty() ? kSRPCGitWorktreeResourceServiceServiceID : service_id) {}

  starpc::Client* SRPCClient() override { return cc_; }

  // GetWorktreeInfo
  virtual starpc::Error GetWorktreeInfo(const s4wave::git::GetWorktreeInfoRequest& in, s4wave::git::GetWorktreeInfoResponse* out) override;
  // GetRepoResource
  virtual starpc::Error GetRepoResource(const s4wave::git::GetRepoResourceRequest& in, s4wave::git::GetRepoResourceResponse* out) override;
  // GetWorkdirResource
  virtual starpc::Error GetWorkdirResource(const s4wave::git::GetWorkdirResourceRequest& in, s4wave::git::GetWorkdirResourceResponse* out) override;
  // WatchStatus
  virtual std::pair<std::unique_ptr<SRPCGitWorktreeResourceService_WatchStatusClient>, starpc::Error> WatchStatus(const s4wave::git::WatchStatusRequest& in) override;
  // StageFiles
  virtual starpc::Error StageFiles(const s4wave::git::StageFilesRequest& in, s4wave::git::StageFilesResponse* out) override;
  // UnstageFiles
  virtual starpc::Error UnstageFiles(const s4wave::git::UnstageFilesRequest& in, s4wave::git::UnstageFilesResponse* out) override;

 private:
  starpc::Client* cc_;
  std::string service_id_;
};

// NewSRPCGitWorktreeResourceServiceClient creates a new client.
inline std::unique_ptr<SRPCGitWorktreeResourceServiceClient> NewSRPCGitWorktreeResourceServiceClient(starpc::Client* cc) {
  return std::make_unique<SRPCGitWorktreeResourceServiceClientImpl>(cc);
}

// SRPCGitWorktreeResourceServiceServer is the server API for GitWorktreeResourceService service.
class SRPCGitWorktreeResourceServiceServer {
 public:
  virtual ~SRPCGitWorktreeResourceServiceServer() = default;

  // GetWorktreeInfo
  virtual starpc::Error GetWorktreeInfo(const s4wave::git::GetWorktreeInfoRequest& req, s4wave::git::GetWorktreeInfoResponse* resp) = 0;
  // GetRepoResource
  virtual starpc::Error GetRepoResource(const s4wave::git::GetRepoResourceRequest& req, s4wave::git::GetRepoResourceResponse* resp) = 0;
  // GetWorkdirResource
  virtual starpc::Error GetWorkdirResource(const s4wave::git::GetWorkdirResourceRequest& req, s4wave::git::GetWorkdirResourceResponse* resp) = 0;
  // WatchStatus
  virtual starpc::Error WatchStatus(const s4wave::git::WatchStatusRequest& req, SRPCGitWorktreeResourceService_WatchStatusStream* strm) = 0;
  // StageFiles
  virtual starpc::Error StageFiles(const s4wave::git::StageFilesRequest& req, s4wave::git::StageFilesResponse* resp) = 0;
  // UnstageFiles
  virtual starpc::Error UnstageFiles(const s4wave::git::UnstageFilesRequest& req, s4wave::git::UnstageFilesResponse* resp) = 0;
};

// SRPCGitWorktreeResourceServiceHandler implements starpc::Handler for GitWorktreeResourceService.
class SRPCGitWorktreeResourceServiceHandler : public starpc::Handler {
 public:
  SRPCGitWorktreeResourceServiceHandler(SRPCGitWorktreeResourceServiceServer* impl, const std::string& service_id = "")
      : impl_(impl), service_id_(service_id.empty() ? kSRPCGitWorktreeResourceServiceServiceID : service_id) {}

  const std::string& GetServiceID() const override { return service_id_; }
  std::vector<std::string> GetMethodIDs() const override;
  std::pair<bool, starpc::Error> InvokeMethod(
      const std::string& service_id,
      const std::string& method_id,
      starpc::Stream* strm) override;

 private:
  SRPCGitWorktreeResourceServiceServer* impl_;
  std::string service_id_;
};

// NewSRPCGitWorktreeResourceServiceHandler creates a new handler for the given implementation.
inline std::unique_ptr<SRPCGitWorktreeResourceServiceHandler> NewSRPCGitWorktreeResourceServiceHandler(SRPCGitWorktreeResourceServiceServer* impl) {
  return std::make_unique<SRPCGitWorktreeResourceServiceHandler>(impl);
}

// SRPCRegisterGitWorktreeResourceService registers the server implementation with the mux.
// The returned handler must outlive the mux registration.
inline std::pair<std::unique_ptr<SRPCGitWorktreeResourceServiceHandler>, starpc::Error> SRPCRegisterGitWorktreeResourceService(starpc::Mux* mux, SRPCGitWorktreeResourceServiceServer* impl) {
  auto handler = NewSRPCGitWorktreeResourceServiceHandler(impl);
  starpc::Error err = mux->Register(handler.get());
  if (err != starpc::Error::OK) {
    return {nullptr, err};
  }
  return {std::move(handler), starpc::Error::OK};
}

// SRPCGitWorktreeResourceService_WatchStatusClient is the client stream for WatchStatus.
class SRPCGitWorktreeResourceService_WatchStatusClient {
 public:
  explicit SRPCGitWorktreeResourceService_WatchStatusClient(std::unique_ptr<starpc::Stream> strm) : strm_(std::move(strm)) {}

  starpc::Error Recv(s4wave::git::WatchStatusResponse* msg) {
    return strm_->MsgRecv(msg);
  }

  starpc::Error CloseSend() { return strm_->CloseSend(); }
  starpc::Error Close() { return strm_->Close(); }

 private:
  std::unique_ptr<starpc::Stream> strm_;
};

// SRPCGitWorktreeResourceService_WatchStatusStream is the server stream for WatchStatus.
class SRPCGitWorktreeResourceService_WatchStatusStream {
 public:
  explicit SRPCGitWorktreeResourceService_WatchStatusStream(starpc::Stream* strm) : strm_(strm) {}

  starpc::Error Send(const s4wave::git::WatchStatusResponse& msg) {
    return strm_->MsgSend(msg);
  }

  starpc::Error SendAndClose(const s4wave::git::WatchStatusResponse& msg) {
    starpc::Error err = strm_->MsgSend(msg);
    if (err != starpc::Error::OK) return err;
    return strm_->CloseSend();
  }

 private:
  starpc::Stream* strm_;
};

}  // namespace s4wave::git
