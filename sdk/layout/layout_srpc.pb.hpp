//go:build deps_only && cgo

// Code generated by protoc-gen-starpc-cpp. DO NOT EDIT.
// protoc-gen-starpc-cpp version: v0.49.5
// source: github.com/s4wave/spacewave/sdk/layout/layout.proto

#pragma once

#include <functional>
#include <memory>
#include <string>
#include <utility>

#include "srpc/starpc.hpp"

#include "layout.pb.h"

namespace s4wave::layout {

// Service ID for LayoutHost
constexpr const char* kSRPCLayoutHostServiceID = "s4wave.layout.LayoutHost";

class SRPCLayoutHost_WatchLayoutModelClient;
class SRPCLayoutHost_WatchLayoutModelStream;

// SRPCLayoutHostClient is the client API for LayoutHost service.
class SRPCLayoutHostClient {
 public:
  virtual ~SRPCLayoutHostClient() = default;

  // SRPCClient returns the underlying SRPC client.
  virtual starpc::Client* SRPCClient() = 0;

  // WatchLayoutModel
  virtual std::pair<std::unique_ptr<SRPCLayoutHost_WatchLayoutModelClient>, starpc::Error> WatchLayoutModel() = 0;
  // NavigateTab
  virtual starpc::Error NavigateTab(const s4wave::layout::NavigateTabRequest& in, s4wave::layout::NavigateTabResponse* out) = 0;
  // AddTab
  virtual starpc::Error AddTab(const s4wave::layout::AddTabRequest& in, s4wave::layout::AddTabResponse* out) = 0;
};

// SRPCLayoutHostClientImpl implements SRPCLayoutHostClient.
class SRPCLayoutHostClientImpl : public SRPCLayoutHostClient {
 public:
  explicit SRPCLayoutHostClientImpl(starpc::Client* cc, const std::string& service_id = "")
      : cc_(cc), service_id_(service_id.empty() ? kSRPCLayoutHostServiceID : service_id) {}

  starpc::Client* SRPCClient() override { return cc_; }

  // WatchLayoutModel
  virtual std::pair<std::unique_ptr<SRPCLayoutHost_WatchLayoutModelClient>, starpc::Error> WatchLayoutModel() override;
  // NavigateTab
  virtual starpc::Error NavigateTab(const s4wave::layout::NavigateTabRequest& in, s4wave::layout::NavigateTabResponse* out) override;
  // AddTab
  virtual starpc::Error AddTab(const s4wave::layout::AddTabRequest& in, s4wave::layout::AddTabResponse* out) override;

 private:
  starpc::Client* cc_;
  std::string service_id_;
};

// NewSRPCLayoutHostClient creates a new client.
inline std::unique_ptr<SRPCLayoutHostClient> NewSRPCLayoutHostClient(starpc::Client* cc) {
  return std::make_unique<SRPCLayoutHostClientImpl>(cc);
}

// SRPCLayoutHostServer is the server API for LayoutHost service.
class SRPCLayoutHostServer {
 public:
  virtual ~SRPCLayoutHostServer() = default;

  // WatchLayoutModel
  virtual starpc::Error WatchLayoutModel(SRPCLayoutHost_WatchLayoutModelStream* strm) = 0;
  // NavigateTab
  virtual starpc::Error NavigateTab(const s4wave::layout::NavigateTabRequest& req, s4wave::layout::NavigateTabResponse* resp) = 0;
  // AddTab
  virtual starpc::Error AddTab(const s4wave::layout::AddTabRequest& req, s4wave::layout::AddTabResponse* resp) = 0;
};

// SRPCLayoutHostHandler implements starpc::Handler for LayoutHost.
class SRPCLayoutHostHandler : public starpc::Handler {
 public:
  SRPCLayoutHostHandler(SRPCLayoutHostServer* impl, const std::string& service_id = "")
      : impl_(impl), service_id_(service_id.empty() ? kSRPCLayoutHostServiceID : service_id) {}

  const std::string& GetServiceID() const override { return service_id_; }
  std::vector<std::string> GetMethodIDs() const override;
  std::pair<bool, starpc::Error> InvokeMethod(
      const std::string& service_id,
      const std::string& method_id,
      starpc::Stream* strm) override;

 private:
  SRPCLayoutHostServer* impl_;
  std::string service_id_;
};

// NewSRPCLayoutHostHandler creates a new handler for the given implementation.
inline std::unique_ptr<SRPCLayoutHostHandler> NewSRPCLayoutHostHandler(SRPCLayoutHostServer* impl) {
  return std::make_unique<SRPCLayoutHostHandler>(impl);
}

// SRPCRegisterLayoutHost registers the server implementation with the mux.
// The returned handler must outlive the mux registration.
inline std::pair<std::unique_ptr<SRPCLayoutHostHandler>, starpc::Error> SRPCRegisterLayoutHost(starpc::Mux* mux, SRPCLayoutHostServer* impl) {
  auto handler = NewSRPCLayoutHostHandler(impl);
  starpc::Error err = mux->Register(handler.get());
  if (err != starpc::Error::OK) {
    return {nullptr, err};
  }
  return {std::move(handler), starpc::Error::OK};
}

// SRPCLayoutHost_WatchLayoutModelClient is the client stream for WatchLayoutModel.
class SRPCLayoutHost_WatchLayoutModelClient {
 public:
  explicit SRPCLayoutHost_WatchLayoutModelClient(std::unique_ptr<starpc::Stream> strm) : strm_(std::move(strm)) {}

  starpc::Error Send(const s4wave::layout::WatchLayoutModelRequest& msg) {
    return strm_->MsgSend(msg);
  }

  starpc::Error Recv(s4wave::layout::LayoutModel* msg) {
    return strm_->MsgRecv(msg);
  }

  starpc::Error CloseSend() { return strm_->CloseSend(); }
  starpc::Error Close() { return strm_->Close(); }

 private:
  std::unique_ptr<starpc::Stream> strm_;
};

// SRPCLayoutHost_WatchLayoutModelStream is the server stream for WatchLayoutModel.
class SRPCLayoutHost_WatchLayoutModelStream {
 public:
  explicit SRPCLayoutHost_WatchLayoutModelStream(starpc::Stream* strm) : strm_(strm) {}

  starpc::Error Send(const s4wave::layout::LayoutModel& msg) {
    return strm_->MsgSend(msg);
  }

  starpc::Error SendAndClose(const s4wave::layout::LayoutModel& msg) {
    starpc::Error err = strm_->MsgSend(msg);
    if (err != starpc::Error::OK) return err;
    return strm_->CloseSend();
  }

  starpc::Error Recv(s4wave::layout::WatchLayoutModelRequest* msg) {
    return strm_->MsgRecv(msg);
  }

 private:
  starpc::Stream* strm_;
};

}  // namespace s4wave::layout
