//go:build deps_only && cgo

// Code generated by protoc-gen-starpc-cpp. DO NOT EDIT.
// protoc-gen-starpc-cpp version: v0.49.7
// source: github.com/s4wave/spacewave/sdk/session/shared-object-self-enrollment.proto

#pragma once

#include <functional>
#include <memory>
#include <string>
#include <utility>

#include "srpc/starpc.hpp"

#include "shared-object-self-enrollment.pb.h"

namespace s4wave::session {

// Service ID for SharedObjectSelfEnrollmentResourceService
constexpr const char* kSRPCSharedObjectSelfEnrollmentResourceServiceServiceID = "s4wave.session.SharedObjectSelfEnrollmentResourceService";

class SRPCSharedObjectSelfEnrollmentResourceService_WatchStateClient;
class SRPCSharedObjectSelfEnrollmentResourceService_WatchStateStream;

// SRPCSharedObjectSelfEnrollmentResourceServiceClient is the client API for SharedObjectSelfEnrollmentResourceService service.
class SRPCSharedObjectSelfEnrollmentResourceServiceClient {
 public:
  virtual ~SRPCSharedObjectSelfEnrollmentResourceServiceClient() = default;

  // SRPCClient returns the underlying SRPC client.
  virtual starpc::Client* SRPCClient() = 0;

  // WatchState
  virtual std::pair<std::unique_ptr<SRPCSharedObjectSelfEnrollmentResourceService_WatchStateClient>, starpc::Error> WatchState(const s4wave::session::WatchSharedObjectSelfEnrollmentStateRequest& in) = 0;
  // Start
  virtual starpc::Error Start(const s4wave::session::StartSharedObjectSelfEnrollmentRequest& in, s4wave::session::StartSharedObjectSelfEnrollmentResponse* out) = 0;
  // Skip
  virtual starpc::Error Skip(const s4wave::session::SkipSharedObjectSelfEnrollmentRequest& in, s4wave::session::SkipSharedObjectSelfEnrollmentResponse* out) = 0;
};

// SRPCSharedObjectSelfEnrollmentResourceServiceClientImpl implements SRPCSharedObjectSelfEnrollmentResourceServiceClient.
class SRPCSharedObjectSelfEnrollmentResourceServiceClientImpl : public SRPCSharedObjectSelfEnrollmentResourceServiceClient {
 public:
  explicit SRPCSharedObjectSelfEnrollmentResourceServiceClientImpl(starpc::Client* cc, const std::string& service_id = "")
      : cc_(cc), service_id_(service_id.empty() ? kSRPCSharedObjectSelfEnrollmentResourceServiceServiceID : service_id) {}

  starpc::Client* SRPCClient() override { return cc_; }

  // WatchState
  virtual std::pair<std::unique_ptr<SRPCSharedObjectSelfEnrollmentResourceService_WatchStateClient>, starpc::Error> WatchState(const s4wave::session::WatchSharedObjectSelfEnrollmentStateRequest& in) override;
  // Start
  virtual starpc::Error Start(const s4wave::session::StartSharedObjectSelfEnrollmentRequest& in, s4wave::session::StartSharedObjectSelfEnrollmentResponse* out) override;
  // Skip
  virtual starpc::Error Skip(const s4wave::session::SkipSharedObjectSelfEnrollmentRequest& in, s4wave::session::SkipSharedObjectSelfEnrollmentResponse* out) override;

 private:
  starpc::Client* cc_;
  std::string service_id_;
};

// NewSRPCSharedObjectSelfEnrollmentResourceServiceClient creates a new client.
inline std::unique_ptr<SRPCSharedObjectSelfEnrollmentResourceServiceClient> NewSRPCSharedObjectSelfEnrollmentResourceServiceClient(starpc::Client* cc) {
  return std::make_unique<SRPCSharedObjectSelfEnrollmentResourceServiceClientImpl>(cc);
}

// SRPCSharedObjectSelfEnrollmentResourceServiceServer is the server API for SharedObjectSelfEnrollmentResourceService service.
class SRPCSharedObjectSelfEnrollmentResourceServiceServer {
 public:
  virtual ~SRPCSharedObjectSelfEnrollmentResourceServiceServer() = default;

  // WatchState
  virtual starpc::Error WatchState(const s4wave::session::WatchSharedObjectSelfEnrollmentStateRequest& req, SRPCSharedObjectSelfEnrollmentResourceService_WatchStateStream* strm) = 0;
  // Start
  virtual starpc::Error Start(const s4wave::session::StartSharedObjectSelfEnrollmentRequest& req, s4wave::session::StartSharedObjectSelfEnrollmentResponse* resp) = 0;
  // Skip
  virtual starpc::Error Skip(const s4wave::session::SkipSharedObjectSelfEnrollmentRequest& req, s4wave::session::SkipSharedObjectSelfEnrollmentResponse* resp) = 0;
};

// SRPCSharedObjectSelfEnrollmentResourceServiceHandler implements starpc::Handler for SharedObjectSelfEnrollmentResourceService.
class SRPCSharedObjectSelfEnrollmentResourceServiceHandler : public starpc::Handler {
 public:
  SRPCSharedObjectSelfEnrollmentResourceServiceHandler(SRPCSharedObjectSelfEnrollmentResourceServiceServer* impl, const std::string& service_id = "")
      : impl_(impl), service_id_(service_id.empty() ? kSRPCSharedObjectSelfEnrollmentResourceServiceServiceID : service_id) {}

  const std::string& GetServiceID() const override { return service_id_; }
  std::vector<std::string> GetMethodIDs() const override;
  std::pair<bool, starpc::Error> InvokeMethod(
      const std::string& service_id,
      const std::string& method_id,
      starpc::Stream* strm) override;

 private:
  SRPCSharedObjectSelfEnrollmentResourceServiceServer* impl_;
  std::string service_id_;
};

// NewSRPCSharedObjectSelfEnrollmentResourceServiceHandler creates a new handler for the given implementation.
inline std::unique_ptr<SRPCSharedObjectSelfEnrollmentResourceServiceHandler> NewSRPCSharedObjectSelfEnrollmentResourceServiceHandler(SRPCSharedObjectSelfEnrollmentResourceServiceServer* impl) {
  return std::make_unique<SRPCSharedObjectSelfEnrollmentResourceServiceHandler>(impl);
}

// SRPCRegisterSharedObjectSelfEnrollmentResourceService registers the server implementation with the mux.
// The returned handler must outlive the mux registration.
inline std::pair<std::unique_ptr<SRPCSharedObjectSelfEnrollmentResourceServiceHandler>, starpc::Error> SRPCRegisterSharedObjectSelfEnrollmentResourceService(starpc::Mux* mux, SRPCSharedObjectSelfEnrollmentResourceServiceServer* impl) {
  auto handler = NewSRPCSharedObjectSelfEnrollmentResourceServiceHandler(impl);
  starpc::Error err = mux->Register(handler.get());
  if (err != starpc::Error::OK) {
    return {nullptr, err};
  }
  return {std::move(handler), starpc::Error::OK};
}

// SRPCSharedObjectSelfEnrollmentResourceService_WatchStateClient is the client stream for WatchState.
class SRPCSharedObjectSelfEnrollmentResourceService_WatchStateClient {
 public:
  explicit SRPCSharedObjectSelfEnrollmentResourceService_WatchStateClient(std::unique_ptr<starpc::Stream> strm) : strm_(std::move(strm)) {}

  starpc::Error Recv(s4wave::session::WatchSharedObjectSelfEnrollmentStateResponse* msg) {
    return strm_->MsgRecv(msg);
  }

  starpc::Error CloseSend() { return strm_->CloseSend(); }
  starpc::Error Close() { return strm_->Close(); }

 private:
  std::unique_ptr<starpc::Stream> strm_;
};

// SRPCSharedObjectSelfEnrollmentResourceService_WatchStateStream is the server stream for WatchState.
class SRPCSharedObjectSelfEnrollmentResourceService_WatchStateStream {
 public:
  explicit SRPCSharedObjectSelfEnrollmentResourceService_WatchStateStream(starpc::Stream* strm) : strm_(strm) {}

  starpc::Error Send(const s4wave::session::WatchSharedObjectSelfEnrollmentStateResponse& msg) {
    return strm_->MsgSend(msg);
  }

  starpc::Error SendAndClose(const s4wave::session::WatchSharedObjectSelfEnrollmentStateResponse& msg) {
    starpc::Error err = strm_->MsgSend(msg);
    if (err != starpc::Error::OK) return err;
    return strm_->CloseSend();
  }

 private:
  starpc::Stream* strm_;
};

}  // namespace s4wave::session
