// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.49.7
// source: github.com/s4wave/spacewave/sdk/space/drive/drive.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for DriveResourceService.
pub const DRIVE_RESOURCE_SERVICE_SERVICE_ID: &str = "s4wave.drive.DriveResourceService";

/// Stream trait for DriveResourceService.WatchDriveState.
#[starpc::async_trait]
pub trait DriveResourceServiceWatchDriveStateStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<WatchDriveStateResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for DriveResourceService.
#[starpc::async_trait]
pub trait DriveResourceServiceClient: Send + Sync {
    /// WatchDriveState.
    async fn watch_drive_state(&self, request: &WatchDriveStateRequest) -> starpc::Result<Box<dyn DriveResourceServiceWatchDriveStateStream>>;
}

/// Client implementation for DriveResourceService.
pub struct DriveResourceServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> DriveResourceServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> DriveResourceServiceClient for DriveResourceServiceClientImpl<C> {
    async fn watch_drive_state(&self, request: &WatchDriveStateRequest) -> starpc::Result<Box<dyn DriveResourceServiceWatchDriveStateStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("s4wave.drive.DriveResourceService", "WatchDriveState", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(DriveResourceServiceWatchDriveStateStreamImpl { stream }))
    }
}

struct DriveResourceServiceWatchDriveStateStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl DriveResourceServiceWatchDriveStateStream for DriveResourceServiceWatchDriveStateStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<WatchDriveStateResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for DriveResourceService.
#[starpc::async_trait]
pub trait DriveResourceServiceServer: Send + Sync {
    /// WatchDriveState.
    async fn watch_drive_state(&self, request: WatchDriveStateRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const DRIVE_RESOURCE_SERVICE_METHOD_IDS: &[&str] = &[
    "WatchDriveState",
];

/// Handler for DriveResourceService.
pub struct DriveResourceServiceHandler<S: DriveResourceServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: DriveResourceServiceServer + 'static> DriveResourceServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: DriveResourceServiceServer + 'static> starpc::Invoker for DriveResourceServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "WatchDriveState" => {
                let request: WatchDriveStateRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.watch_drive_state(request, stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: DriveResourceServiceServer + 'static> starpc::Handler for DriveResourceServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "s4wave.drive.DriveResourceService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        DRIVE_RESOURCE_SERVICE_METHOD_IDS
    }
}

