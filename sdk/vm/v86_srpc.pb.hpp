//go:build deps_only && cgo

// Code generated by protoc-gen-starpc-cpp. DO NOT EDIT.
// protoc-gen-starpc-cpp version: v0.49.18
// source: github.com/s4wave/spacewave/sdk/vm/v86.proto

#pragma once

#include <functional>
#include <memory>
#include <string>
#include <utility>

#include "srpc/starpc.hpp"

#include "v86.pb.h"

namespace s4wave::vm {

// Service ID for V86RuntimeStatusService
constexpr const char* kSRPCV86RuntimeStatusServiceServiceID = "s4wave.vm.V86RuntimeStatusService";


// SRPCV86RuntimeStatusServiceClient is the client API for V86RuntimeStatusService service.
class SRPCV86RuntimeStatusServiceClient {
 public:
  virtual ~SRPCV86RuntimeStatusServiceClient() = default;

  // SRPCClient returns the underlying SRPC client.
  virtual starpc::Client* SRPCClient() = 0;

  // ReportStatus
  virtual starpc::Error ReportStatus(const s4wave::vm::ReportV86RuntimeStatusRequest& in, s4wave::vm::ReportV86RuntimeStatusResponse* out) = 0;
};

// SRPCV86RuntimeStatusServiceClientImpl implements SRPCV86RuntimeStatusServiceClient.
class SRPCV86RuntimeStatusServiceClientImpl : public SRPCV86RuntimeStatusServiceClient {
 public:
  explicit SRPCV86RuntimeStatusServiceClientImpl(starpc::Client* cc, const std::string& service_id = "")
      : cc_(cc), service_id_(service_id.empty() ? kSRPCV86RuntimeStatusServiceServiceID : service_id) {}

  starpc::Client* SRPCClient() override { return cc_; }

  // ReportStatus
  virtual starpc::Error ReportStatus(const s4wave::vm::ReportV86RuntimeStatusRequest& in, s4wave::vm::ReportV86RuntimeStatusResponse* out) override;

 private:
  starpc::Client* cc_;
  std::string service_id_;
};

// NewSRPCV86RuntimeStatusServiceClient creates a new client.
inline std::unique_ptr<SRPCV86RuntimeStatusServiceClient> NewSRPCV86RuntimeStatusServiceClient(starpc::Client* cc) {
  return std::make_unique<SRPCV86RuntimeStatusServiceClientImpl>(cc);
}

// SRPCV86RuntimeStatusServiceServer is the server API for V86RuntimeStatusService service.
class SRPCV86RuntimeStatusServiceServer {
 public:
  virtual ~SRPCV86RuntimeStatusServiceServer() = default;

  // ReportStatus
  virtual starpc::Error ReportStatus(const s4wave::vm::ReportV86RuntimeStatusRequest& req, s4wave::vm::ReportV86RuntimeStatusResponse* resp) = 0;
};

// SRPCV86RuntimeStatusServiceHandler implements starpc::Handler for V86RuntimeStatusService.
class SRPCV86RuntimeStatusServiceHandler : public starpc::Handler {
 public:
  SRPCV86RuntimeStatusServiceHandler(SRPCV86RuntimeStatusServiceServer* impl, const std::string& service_id = "")
      : impl_(impl), service_id_(service_id.empty() ? kSRPCV86RuntimeStatusServiceServiceID : service_id) {}

  const std::string& GetServiceID() const override { return service_id_; }
  std::vector<std::string> GetMethodIDs() const override;
  std::pair<bool, starpc::Error> InvokeMethod(
      const std::string& service_id,
      const std::string& method_id,
      starpc::Stream* strm) override;

 private:
  SRPCV86RuntimeStatusServiceServer* impl_;
  std::string service_id_;
};

// NewSRPCV86RuntimeStatusServiceHandler creates a new handler for the given implementation.
inline std::unique_ptr<SRPCV86RuntimeStatusServiceHandler> NewSRPCV86RuntimeStatusServiceHandler(SRPCV86RuntimeStatusServiceServer* impl) {
  return std::make_unique<SRPCV86RuntimeStatusServiceHandler>(impl);
}

// SRPCRegisterV86RuntimeStatusService registers the server implementation with the mux.
// The returned handler must outlive the mux registration.
inline std::pair<std::unique_ptr<SRPCV86RuntimeStatusServiceHandler>, starpc::Error> SRPCRegisterV86RuntimeStatusService(starpc::Mux* mux, SRPCV86RuntimeStatusServiceServer* impl) {
  auto handler = NewSRPCV86RuntimeStatusServiceHandler(impl);
  starpc::Error err = mux->Register(handler.get());
  if (err != starpc::Error::OK) {
    return {nullptr, err};
  }
  return {std::move(handler), starpc::Error::OK};
}

}  // namespace s4wave::vm
