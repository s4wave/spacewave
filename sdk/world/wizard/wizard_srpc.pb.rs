// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.49.7
// source: github.com/s4wave/spacewave/sdk/world/wizard/wizard.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for ObjectWizardRegistryResourceService.
pub const OBJECT_WIZARD_REGISTRY_RESOURCE_SERVICE_SERVICE_ID: &str = "s4wave.wizard.ObjectWizardRegistryResourceService";

/// Client trait for ObjectWizardRegistryResourceService.
#[starpc::async_trait]
pub trait ObjectWizardRegistryResourceServiceClient: Send + Sync {
    /// ListWizards.
    async fn list_wizards(&self, request: &ListWizardsRequest) -> starpc::Result<ListWizardsResponse>;
}

/// Client implementation for ObjectWizardRegistryResourceService.
pub struct ObjectWizardRegistryResourceServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> ObjectWizardRegistryResourceServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> ObjectWizardRegistryResourceServiceClient for ObjectWizardRegistryResourceServiceClientImpl<C> {
    async fn list_wizards(&self, request: &ListWizardsRequest) -> starpc::Result<ListWizardsResponse> {
        self.client.exec_call("s4wave.wizard.ObjectWizardRegistryResourceService", "ListWizards", request).await
    }
}

/// Server trait for ObjectWizardRegistryResourceService.
#[starpc::async_trait]
pub trait ObjectWizardRegistryResourceServiceServer: Send + Sync {
    /// ListWizards.
    async fn list_wizards(&self, request: ListWizardsRequest) -> starpc::Result<ListWizardsResponse>;
}

const OBJECT_WIZARD_REGISTRY_RESOURCE_SERVICE_METHOD_IDS: &[&str] = &[
    "ListWizards",
];

/// Handler for ObjectWizardRegistryResourceService.
pub struct ObjectWizardRegistryResourceServiceHandler<S: ObjectWizardRegistryResourceServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: ObjectWizardRegistryResourceServiceServer + 'static> ObjectWizardRegistryResourceServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: ObjectWizardRegistryResourceServiceServer + 'static> starpc::Invoker for ObjectWizardRegistryResourceServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "ListWizards" => {
                let request: ListWizardsRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.list_wizards(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: ObjectWizardRegistryResourceServiceServer + 'static> starpc::Handler for ObjectWizardRegistryResourceServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "s4wave.wizard.ObjectWizardRegistryResourceService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        OBJECT_WIZARD_REGISTRY_RESOURCE_SERVICE_METHOD_IDS
    }
}

/// Service ID for WizardResourceService.
pub const WIZARD_RESOURCE_SERVICE_SERVICE_ID: &str = "s4wave.wizard.WizardResourceService";

/// Stream trait for WizardResourceService.WatchWizardState.
#[starpc::async_trait]
pub trait WizardResourceServiceWatchWizardStateStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<WatchWizardStateResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Stream trait for WizardResourceService.WatchGitCloneProgress.
#[starpc::async_trait]
pub trait WizardResourceServiceWatchGitCloneProgressStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<WatchGitCloneProgressResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for WizardResourceService.
#[starpc::async_trait]
pub trait WizardResourceServiceClient: Send + Sync {
    /// WatchWizardState.
    async fn watch_wizard_state(&self, request: &WatchWizardStateRequest) -> starpc::Result<Box<dyn WizardResourceServiceWatchWizardStateStream>>;
    /// UpdateWizardState.
    async fn update_wizard_state(&self, request: &UpdateWizardStateRequest) -> starpc::Result<UpdateWizardStateResponse>;
    /// StartGitClone.
    async fn start_git_clone(&self, request: &StartGitCloneRequest) -> starpc::Result<StartGitCloneResponse>;
    /// WatchGitCloneProgress.
    async fn watch_git_clone_progress(&self, request: &WatchGitCloneProgressRequest) -> starpc::Result<Box<dyn WizardResourceServiceWatchGitCloneProgressStream>>;
}

/// Client implementation for WizardResourceService.
pub struct WizardResourceServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> WizardResourceServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> WizardResourceServiceClient for WizardResourceServiceClientImpl<C> {
    async fn watch_wizard_state(&self, request: &WatchWizardStateRequest) -> starpc::Result<Box<dyn WizardResourceServiceWatchWizardStateStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("s4wave.wizard.WizardResourceService", "WatchWizardState", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(WizardResourceServiceWatchWizardStateStreamImpl { stream }))
    }
    async fn update_wizard_state(&self, request: &UpdateWizardStateRequest) -> starpc::Result<UpdateWizardStateResponse> {
        self.client.exec_call("s4wave.wizard.WizardResourceService", "UpdateWizardState", request).await
    }
    async fn start_git_clone(&self, request: &StartGitCloneRequest) -> starpc::Result<StartGitCloneResponse> {
        self.client.exec_call("s4wave.wizard.WizardResourceService", "StartGitClone", request).await
    }
    async fn watch_git_clone_progress(&self, request: &WatchGitCloneProgressRequest) -> starpc::Result<Box<dyn WizardResourceServiceWatchGitCloneProgressStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("s4wave.wizard.WizardResourceService", "WatchGitCloneProgress", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(WizardResourceServiceWatchGitCloneProgressStreamImpl { stream }))
    }
}

struct WizardResourceServiceWatchWizardStateStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl WizardResourceServiceWatchWizardStateStream for WizardResourceServiceWatchWizardStateStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<WatchWizardStateResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

struct WizardResourceServiceWatchGitCloneProgressStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl WizardResourceServiceWatchGitCloneProgressStream for WizardResourceServiceWatchGitCloneProgressStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<WatchGitCloneProgressResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for WizardResourceService.
#[starpc::async_trait]
pub trait WizardResourceServiceServer: Send + Sync {
    /// WatchWizardState.
    async fn watch_wizard_state(&self, request: WatchWizardStateRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// UpdateWizardState.
    async fn update_wizard_state(&self, request: UpdateWizardStateRequest) -> starpc::Result<UpdateWizardStateResponse>;
    /// StartGitClone.
    async fn start_git_clone(&self, request: StartGitCloneRequest) -> starpc::Result<StartGitCloneResponse>;
    /// WatchGitCloneProgress.
    async fn watch_git_clone_progress(&self, request: WatchGitCloneProgressRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const WIZARD_RESOURCE_SERVICE_METHOD_IDS: &[&str] = &[
    "WatchWizardState",
    "UpdateWizardState",
    "StartGitClone",
    "WatchGitCloneProgress",
];

/// Handler for WizardResourceService.
pub struct WizardResourceServiceHandler<S: WizardResourceServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: WizardResourceServiceServer + 'static> WizardResourceServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: WizardResourceServiceServer + 'static> starpc::Invoker for WizardResourceServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "WatchWizardState" => {
                let request: WatchWizardStateRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.watch_wizard_state(request, stream).await)
            }
            "UpdateWizardState" => {
                let request: UpdateWizardStateRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.update_wizard_state(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "StartGitClone" => {
                let request: StartGitCloneRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.start_git_clone(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "WatchGitCloneProgress" => {
                let request: WatchGitCloneProgressRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.watch_git_clone_progress(request, stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: WizardResourceServiceServer + 'static> starpc::Handler for WizardResourceServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "s4wave.wizard.WizardResourceService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        WIZARD_RESOURCE_SERVICE_METHOD_IDS
    }
}

