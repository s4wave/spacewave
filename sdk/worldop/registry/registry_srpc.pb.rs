// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.49.5
// source: github.com/s4wave/spacewave/sdk/worldop/registry/registry.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for WorldOpRegistryResourceService.
pub const WORLD_OP_REGISTRY_RESOURCE_SERVICE_SERVICE_ID: &str = "s4wave.worldop.registry.WorldOpRegistryResourceService";

/// Stream trait for WorldOpRegistryResourceService.WatchWorldOps.
#[starpc::async_trait]
pub trait WorldOpRegistryResourceServiceWatchWorldOpsStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<WatchWorldOpsResponse>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for WorldOpRegistryResourceService.
#[starpc::async_trait]
pub trait WorldOpRegistryResourceServiceClient: Send + Sync {
    /// RegisterWorldOp.
    async fn register_world_op(&self, request: &RegisterWorldOpRequest) -> starpc::Result<RegisterWorldOpResponse>;
    /// WatchWorldOps.
    async fn watch_world_ops(&self, request: &WatchWorldOpsRequest) -> starpc::Result<Box<dyn WorldOpRegistryResourceServiceWatchWorldOpsStream>>;
}

/// Client implementation for WorldOpRegistryResourceService.
pub struct WorldOpRegistryResourceServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> WorldOpRegistryResourceServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> WorldOpRegistryResourceServiceClient for WorldOpRegistryResourceServiceClientImpl<C> {
    async fn register_world_op(&self, request: &RegisterWorldOpRequest) -> starpc::Result<RegisterWorldOpResponse> {
        self.client.exec_call("s4wave.worldop.registry.WorldOpRegistryResourceService", "RegisterWorldOp", request).await
    }
    async fn watch_world_ops(&self, request: &WatchWorldOpsRequest) -> starpc::Result<Box<dyn WorldOpRegistryResourceServiceWatchWorldOpsStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("s4wave.worldop.registry.WorldOpRegistryResourceService", "WatchWorldOps", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(WorldOpRegistryResourceServiceWatchWorldOpsStreamImpl { stream }))
    }
}

struct WorldOpRegistryResourceServiceWatchWorldOpsStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl WorldOpRegistryResourceServiceWatchWorldOpsStream for WorldOpRegistryResourceServiceWatchWorldOpsStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<WatchWorldOpsResponse> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for WorldOpRegistryResourceService.
#[starpc::async_trait]
pub trait WorldOpRegistryResourceServiceServer: Send + Sync {
    /// RegisterWorldOp.
    async fn register_world_op(&self, request: RegisterWorldOpRequest) -> starpc::Result<RegisterWorldOpResponse>;
    /// WatchWorldOps.
    async fn watch_world_ops(&self, request: WatchWorldOpsRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const WORLD_OP_REGISTRY_RESOURCE_SERVICE_METHOD_IDS: &[&str] = &[
    "RegisterWorldOp",
    "WatchWorldOps",
];

/// Handler for WorldOpRegistryResourceService.
pub struct WorldOpRegistryResourceServiceHandler<S: WorldOpRegistryResourceServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: WorldOpRegistryResourceServiceServer + 'static> WorldOpRegistryResourceServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: WorldOpRegistryResourceServiceServer + 'static> starpc::Invoker for WorldOpRegistryResourceServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "RegisterWorldOp" => {
                let request: RegisterWorldOpRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.register_world_op(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "WatchWorldOps" => {
                let request: WatchWorldOpsRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.watch_world_ops(request, stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: WorldOpRegistryResourceServiceServer + 'static> starpc::Handler for WorldOpRegistryResourceServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "s4wave.worldop.registry.WorldOpRegistryResourceService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        WORLD_OP_REGISTRY_RESOURCE_SERVICE_METHOD_IDS
    }
}

/// Service ID for WorldOpHandlerService.
pub const WORLD_OP_HANDLER_SERVICE_SERVICE_ID: &str = "s4wave.worldop.registry.WorldOpHandlerService";

/// Client trait for WorldOpHandlerService.
#[starpc::async_trait]
pub trait WorldOpHandlerServiceClient: Send + Sync {
    /// ApplyWorldOp.
    async fn apply_world_op(&self, request: &ApplyWorldOpRequest) -> starpc::Result<ApplyWorldOpResponse>;
    /// ApplyWorldObjectOp.
    async fn apply_world_object_op(&self, request: &ApplyWorldObjectOpRequest) -> starpc::Result<ApplyWorldObjectOpResponse>;
    /// ValidateOp.
    async fn validate_op(&self, request: &ValidateOpRequest) -> starpc::Result<ValidateOpResponse>;
}

/// Client implementation for WorldOpHandlerService.
pub struct WorldOpHandlerServiceClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> WorldOpHandlerServiceClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> WorldOpHandlerServiceClient for WorldOpHandlerServiceClientImpl<C> {
    async fn apply_world_op(&self, request: &ApplyWorldOpRequest) -> starpc::Result<ApplyWorldOpResponse> {
        self.client.exec_call("s4wave.worldop.registry.WorldOpHandlerService", "ApplyWorldOp", request).await
    }
    async fn apply_world_object_op(&self, request: &ApplyWorldObjectOpRequest) -> starpc::Result<ApplyWorldObjectOpResponse> {
        self.client.exec_call("s4wave.worldop.registry.WorldOpHandlerService", "ApplyWorldObjectOp", request).await
    }
    async fn validate_op(&self, request: &ValidateOpRequest) -> starpc::Result<ValidateOpResponse> {
        self.client.exec_call("s4wave.worldop.registry.WorldOpHandlerService", "ValidateOp", request).await
    }
}

/// Server trait for WorldOpHandlerService.
#[starpc::async_trait]
pub trait WorldOpHandlerServiceServer: Send + Sync {
    /// ApplyWorldOp.
    async fn apply_world_op(&self, request: ApplyWorldOpRequest) -> starpc::Result<ApplyWorldOpResponse>;
    /// ApplyWorldObjectOp.
    async fn apply_world_object_op(&self, request: ApplyWorldObjectOpRequest) -> starpc::Result<ApplyWorldObjectOpResponse>;
    /// ValidateOp.
    async fn validate_op(&self, request: ValidateOpRequest) -> starpc::Result<ValidateOpResponse>;
}

const WORLD_OP_HANDLER_SERVICE_METHOD_IDS: &[&str] = &[
    "ApplyWorldOp",
    "ApplyWorldObjectOp",
    "ValidateOp",
];

/// Handler for WorldOpHandlerService.
pub struct WorldOpHandlerServiceHandler<S: WorldOpHandlerServiceServer> {
    server: std::sync::Arc<S>,
}

impl<S: WorldOpHandlerServiceServer + 'static> WorldOpHandlerServiceHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: WorldOpHandlerServiceServer + 'static> starpc::Invoker for WorldOpHandlerServiceHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "ApplyWorldOp" => {
                let request: ApplyWorldOpRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.apply_world_op(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "ApplyWorldObjectOp" => {
                let request: ApplyWorldObjectOpRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.apply_world_object_op(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "ValidateOp" => {
                let request: ValidateOpRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.validate_op(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: WorldOpHandlerServiceServer + 'static> starpc::Handler for WorldOpHandlerServiceHandler<S> {
    fn service_id(&self) -> &'static str {
        "s4wave.worldop.registry.WorldOpHandlerService"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        WORLD_OP_HANDLER_SERVICE_METHOD_IDS
    }
}

