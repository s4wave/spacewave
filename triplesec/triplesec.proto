syntax = "proto3";
package auth.triplesec;

// Params are parameters to the triplesec auth.
// These are stored along with the account object.
message Params {
  // Salt is the salt to use.
  bytes salt = 1;
  // Version is the triplesec version to use.
  // If zero, latest is used.
  uint32 version = 2;
}