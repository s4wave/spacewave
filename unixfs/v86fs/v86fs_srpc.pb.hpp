//go:build deps_only && cgo

// Code generated by protoc-gen-starpc-cpp. DO NOT EDIT.
// protoc-gen-starpc-cpp version: v0.47.1
// source: github.com/aperturerobotics/hydra/unixfs/v86fs/v86fs.proto

#pragma once

#include <functional>
#include <memory>
#include <string>
#include <utility>

#include "srpc/starpc.hpp"

#include "v86fs.pb.h"

namespace unixfs::v86fs {

// Service ID for V86fsService
constexpr const char* kSRPCV86fsServiceServiceID = "unixfs.v86fs.V86fsService";

class SRPCV86fsService_RelayV86fsClient;
class SRPCV86fsService_RelayV86fsStream;

// SRPCV86fsServiceClient is the client API for V86fsService service.
class SRPCV86fsServiceClient {
 public:
  virtual ~SRPCV86fsServiceClient() = default;

  // SRPCClient returns the underlying SRPC client.
  virtual starpc::Client* SRPCClient() = 0;

  // RelayV86fs
  virtual std::pair<std::unique_ptr<SRPCV86fsService_RelayV86fsClient>, starpc::Error> RelayV86fs() = 0;
};

// SRPCV86fsServiceClientImpl implements SRPCV86fsServiceClient.
class SRPCV86fsServiceClientImpl : public SRPCV86fsServiceClient {
 public:
  explicit SRPCV86fsServiceClientImpl(starpc::Client* cc, const std::string& service_id = "")
      : cc_(cc), service_id_(service_id.empty() ? kSRPCV86fsServiceServiceID : service_id) {}

  starpc::Client* SRPCClient() override { return cc_; }

  // RelayV86fs
  virtual std::pair<std::unique_ptr<SRPCV86fsService_RelayV86fsClient>, starpc::Error> RelayV86fs() override;

 private:
  starpc::Client* cc_;
  std::string service_id_;
};

// NewSRPCV86fsServiceClient creates a new client.
inline std::unique_ptr<SRPCV86fsServiceClient> NewSRPCV86fsServiceClient(starpc::Client* cc) {
  return std::make_unique<SRPCV86fsServiceClientImpl>(cc);
}

// SRPCV86fsServiceServer is the server API for V86fsService service.
class SRPCV86fsServiceServer {
 public:
  virtual ~SRPCV86fsServiceServer() = default;

  // RelayV86fs
  virtual starpc::Error RelayV86fs(SRPCV86fsService_RelayV86fsStream* strm) = 0;
};

// SRPCV86fsServiceHandler implements starpc::Handler for V86fsService.
class SRPCV86fsServiceHandler : public starpc::Handler {
 public:
  SRPCV86fsServiceHandler(SRPCV86fsServiceServer* impl, const std::string& service_id = "")
      : impl_(impl), service_id_(service_id.empty() ? kSRPCV86fsServiceServiceID : service_id) {}

  const std::string& GetServiceID() const override { return service_id_; }
  std::vector<std::string> GetMethodIDs() const override;
  std::pair<bool, starpc::Error> InvokeMethod(
      const std::string& service_id,
      const std::string& method_id,
      starpc::Stream* strm) override;

 private:
  SRPCV86fsServiceServer* impl_;
  std::string service_id_;
};

// NewSRPCV86fsServiceHandler creates a new handler for the given implementation.
inline std::unique_ptr<SRPCV86fsServiceHandler> NewSRPCV86fsServiceHandler(SRPCV86fsServiceServer* impl) {
  return std::make_unique<SRPCV86fsServiceHandler>(impl);
}

// SRPCRegisterV86fsService registers the server implementation with the mux.
// The returned handler must outlive the mux registration.
inline std::pair<std::unique_ptr<SRPCV86fsServiceHandler>, starpc::Error> SRPCRegisterV86fsService(starpc::Mux* mux, SRPCV86fsServiceServer* impl) {
  auto handler = NewSRPCV86fsServiceHandler(impl);
  starpc::Error err = mux->Register(handler.get());
  if (err != starpc::Error::OK) {
    return {nullptr, err};
  }
  return {std::move(handler), starpc::Error::OK};
}

// SRPCV86fsService_RelayV86fsClient is the client stream for RelayV86fs.
class SRPCV86fsService_RelayV86fsClient {
 public:
  explicit SRPCV86fsService_RelayV86fsClient(std::unique_ptr<starpc::Stream> strm) : strm_(std::move(strm)) {}

  starpc::Error Send(const unixfs::v86fs::V86fsMessage& msg) {
    return strm_->MsgSend(msg);
  }

  starpc::Error Recv(unixfs::v86fs::V86fsMessage* msg) {
    return strm_->MsgRecv(msg);
  }

  starpc::Error CloseSend() { return strm_->CloseSend(); }
  starpc::Error Close() { return strm_->Close(); }

 private:
  std::unique_ptr<starpc::Stream> strm_;
};

// SRPCV86fsService_RelayV86fsStream is the server stream for RelayV86fs.
class SRPCV86fsService_RelayV86fsStream {
 public:
  explicit SRPCV86fsService_RelayV86fsStream(starpc::Stream* strm) : strm_(strm) {}

  starpc::Error Send(const unixfs::v86fs::V86fsMessage& msg) {
    return strm_->MsgSend(msg);
  }

  starpc::Error SendAndClose(const unixfs::v86fs::V86fsMessage& msg) {
    starpc::Error err = strm_->MsgSend(msg);
    if (err != starpc::Error::OK) return err;
    return strm_->CloseSend();
  }

  starpc::Error Recv(unixfs::v86fs::V86fsMessage* msg) {
    return strm_->MsgRecv(msg);
  }

 private:
  starpc::Stream* strm_;
};

}  // namespace unixfs::v86fs
