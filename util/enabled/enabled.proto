syntax = "proto3";
package enabled;

// Enabled is a three-way boolean: default, enable, disable.
enum Enabled {
  // CompressionMode_DEFAULT defaults to the default option.
  DEFAULT = 0;
  // CompressionMode_ENABLE enables the option even if it would normally be false.
  ENABLE = 1;
  // CompressionMode_DISABLE disables the option even if it would normally be true.
  DISABLE = 2;
}
