// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.46.1
// source: github.com/aperturerobotics/bldr/web/bundler/vite/vite.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for ViteBundler.
pub const VITE_BUNDLER_SERVICE_ID: &str = "bldr.web.bundler.vite.ViteBundler";

/// Client trait for ViteBundler.
#[starpc::async_trait]
pub trait ViteBundlerClient: Send + Sync {
    /// Build.
    async fn build(&self, request: &BuildRequest) -> starpc::Result<BuildResponse>;
}

/// Client implementation for ViteBundler.
pub struct ViteBundlerClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> ViteBundlerClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> ViteBundlerClient for ViteBundlerClientImpl<C> {
    async fn build(&self, request: &BuildRequest) -> starpc::Result<BuildResponse> {
        self.client.exec_call("bldr.web.bundler.vite.ViteBundler", "Build", request).await
    }
}

/// Server trait for ViteBundler.
#[starpc::async_trait]
pub trait ViteBundlerServer: Send + Sync {
    /// Build.
    async fn build(&self, request: BuildRequest) -> starpc::Result<BuildResponse>;
}

const VITE_BUNDLER_METHOD_IDS: &[&str] = &[
    "Build",
];

/// Handler for ViteBundler.
pub struct ViteBundlerHandler<S: ViteBundlerServer> {
    server: std::sync::Arc<S>,
}

impl<S: ViteBundlerServer + 'static> ViteBundlerHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: ViteBundlerServer + 'static> starpc::Invoker for ViteBundlerHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "Build" => {
                let request: BuildRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.build(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: ViteBundlerServer + 'static> starpc::Handler for ViteBundlerHandler<S> {
    fn service_id(&self) -> &'static str {
        "bldr.web.bundler.vite.ViteBundler"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        VITE_BUNDLER_METHOD_IDS
    }
}

