// Code generated by protoc-gen-starpc-rust. DO NOT EDIT.
// protoc-gen-starpc-rust version: v0.46.1
// source: github.com/aperturerobotics/bldr/web/runtime/runtime.proto

#[allow(unused_imports)]
use starpc::StreamExt;

/// Service ID for WebRuntimeHost.
pub const WEB_RUNTIME_HOST_SERVICE_ID: &str = "web.runtime.WebRuntimeHost";

/// Stream trait for WebRuntimeHost.WebDocumentRpc.
#[starpc::async_trait]
pub trait WebRuntimeHostWebDocumentRpcStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<RpcStreamPacket>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Stream trait for WebRuntimeHost.ServiceWorkerRpc.
#[starpc::async_trait]
pub trait WebRuntimeHostServiceWorkerRpcStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<RpcStreamPacket>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Stream trait for WebRuntimeHost.WebWorkerRpc.
#[starpc::async_trait]
pub trait WebRuntimeHostWebWorkerRpcStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<RpcStreamPacket>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for WebRuntimeHost.
#[starpc::async_trait]
pub trait WebRuntimeHostClient: Send + Sync {
    /// WebDocumentRpc.
    async fn web_document_rpc(&self) -> starpc::Result<Box<dyn WebRuntimeHostWebDocumentRpcStream>>;
    /// ServiceWorkerRpc.
    async fn service_worker_rpc(&self) -> starpc::Result<Box<dyn WebRuntimeHostServiceWorkerRpcStream>>;
    /// WebWorkerRpc.
    async fn web_worker_rpc(&self) -> starpc::Result<Box<dyn WebRuntimeHostWebWorkerRpcStream>>;
}

/// Client implementation for WebRuntimeHost.
pub struct WebRuntimeHostClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> WebRuntimeHostClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> WebRuntimeHostClient for WebRuntimeHostClientImpl<C> {
    async fn web_document_rpc(&self) -> starpc::Result<Box<dyn WebRuntimeHostWebDocumentRpcStream>> {
        let stream = self.client.new_stream("web.runtime.WebRuntimeHost", "WebDocumentRpc", None).await?;
        Ok(Box::new(WebRuntimeHostWebDocumentRpcStreamImpl { stream }))
    }
    async fn service_worker_rpc(&self) -> starpc::Result<Box<dyn WebRuntimeHostServiceWorkerRpcStream>> {
        let stream = self.client.new_stream("web.runtime.WebRuntimeHost", "ServiceWorkerRpc", None).await?;
        Ok(Box::new(WebRuntimeHostServiceWorkerRpcStreamImpl { stream }))
    }
    async fn web_worker_rpc(&self) -> starpc::Result<Box<dyn WebRuntimeHostWebWorkerRpcStream>> {
        let stream = self.client.new_stream("web.runtime.WebRuntimeHost", "WebWorkerRpc", None).await?;
        Ok(Box::new(WebRuntimeHostWebWorkerRpcStreamImpl { stream }))
    }
}

struct WebRuntimeHostWebDocumentRpcStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl WebRuntimeHostWebDocumentRpcStream for WebRuntimeHostWebDocumentRpcStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<RpcStreamPacket> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

struct WebRuntimeHostServiceWorkerRpcStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl WebRuntimeHostServiceWorkerRpcStream for WebRuntimeHostServiceWorkerRpcStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<RpcStreamPacket> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

struct WebRuntimeHostWebWorkerRpcStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl WebRuntimeHostWebWorkerRpcStream for WebRuntimeHostWebWorkerRpcStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<RpcStreamPacket> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for WebRuntimeHost.
#[starpc::async_trait]
pub trait WebRuntimeHostServer: Send + Sync {
    /// WebDocumentRpc.
    async fn web_document_rpc(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// ServiceWorkerRpc.
    async fn service_worker_rpc(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// WebWorkerRpc.
    async fn web_worker_rpc(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const WEB_RUNTIME_HOST_METHOD_IDS: &[&str] = &[
    "WebDocumentRpc",
    "ServiceWorkerRpc",
    "WebWorkerRpc",
];

/// Handler for WebRuntimeHost.
pub struct WebRuntimeHostHandler<S: WebRuntimeHostServer> {
    server: std::sync::Arc<S>,
}

impl<S: WebRuntimeHostServer + 'static> WebRuntimeHostHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: WebRuntimeHostServer + 'static> starpc::Invoker for WebRuntimeHostHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "WebDocumentRpc" => {
                (true, self.server.web_document_rpc(stream).await)
            }
            "ServiceWorkerRpc" => {
                (true, self.server.service_worker_rpc(stream).await)
            }
            "WebWorkerRpc" => {
                (true, self.server.web_worker_rpc(stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: WebRuntimeHostServer + 'static> starpc::Handler for WebRuntimeHostHandler<S> {
    fn service_id(&self) -> &'static str {
        "web.runtime.WebRuntimeHost"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        WEB_RUNTIME_HOST_METHOD_IDS
    }
}

/// Service ID for WebRuntime.
pub const WEB_RUNTIME_SERVICE_ID: &str = "web.runtime.WebRuntime";

/// Stream trait for WebRuntime.WatchWebRuntimeStatus.
#[starpc::async_trait]
pub trait WebRuntimeWatchWebRuntimeStatusStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<WebRuntimeStatus>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Stream trait for WebRuntime.WebDocumentRpc.
#[starpc::async_trait]
pub trait WebRuntimeWebDocumentRpcStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<RpcStreamPacket>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Stream trait for WebRuntime.WebWorkerRpc.
#[starpc::async_trait]
pub trait WebRuntimeWebWorkerRpcStream: Send + Sync {
    /// Returns the context for this stream.
    fn context(&self) -> &starpc::Context;
    /// Sends a message on the stream.
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()>;
    /// Receives a message from the stream.
    async fn recv(&self) -> starpc::Result<RpcStreamPacket>;
    /// Closes the stream.
    async fn close(&self) -> starpc::Result<()>;
}

/// Client trait for WebRuntime.
#[starpc::async_trait]
pub trait WebRuntimeClient: Send + Sync {
    /// WatchWebRuntimeStatus.
    async fn watch_web_runtime_status(&self, request: &WatchWebRuntimeStatusRequest) -> starpc::Result<Box<dyn WebRuntimeWatchWebRuntimeStatusStream>>;
    /// CreateWebDocument.
    async fn create_web_document(&self, request: &CreateWebDocumentRequest) -> starpc::Result<CreateWebDocumentResponse>;
    /// RemoveWebDocument.
    async fn remove_web_document(&self, request: &RemoveWebDocumentRequest) -> starpc::Result<RemoveWebDocumentResponse>;
    /// WebDocumentRpc.
    async fn web_document_rpc(&self) -> starpc::Result<Box<dyn WebRuntimeWebDocumentRpcStream>>;
    /// WebWorkerRpc.
    async fn web_worker_rpc(&self) -> starpc::Result<Box<dyn WebRuntimeWebWorkerRpcStream>>;
}

/// Client implementation for WebRuntime.
pub struct WebRuntimeClientImpl<C> {
    client: C,
}

impl<C: starpc::Client> WebRuntimeClientImpl<C> {
    /// Creates a new client.
    pub fn new(client: C) -> Self {
        Self { client }
    }
}

#[starpc::async_trait]
impl<C: starpc::Client + 'static> WebRuntimeClient for WebRuntimeClientImpl<C> {
    async fn watch_web_runtime_status(&self, request: &WatchWebRuntimeStatusRequest) -> starpc::Result<Box<dyn WebRuntimeWatchWebRuntimeStatusStream>> {
        use starpc::ProstMessage;
        let data = request.encode_to_vec();
        let stream = self.client.new_stream("web.runtime.WebRuntime", "WatchWebRuntimeStatus", Some(&data)).await?;
        stream.close_send().await?;
        Ok(Box::new(WebRuntimeWatchWebRuntimeStatusStreamImpl { stream }))
    }
    async fn create_web_document(&self, request: &CreateWebDocumentRequest) -> starpc::Result<CreateWebDocumentResponse> {
        self.client.exec_call("web.runtime.WebRuntime", "CreateWebDocument", request).await
    }
    async fn remove_web_document(&self, request: &RemoveWebDocumentRequest) -> starpc::Result<RemoveWebDocumentResponse> {
        self.client.exec_call("web.runtime.WebRuntime", "RemoveWebDocument", request).await
    }
    async fn web_document_rpc(&self) -> starpc::Result<Box<dyn WebRuntimeWebDocumentRpcStream>> {
        let stream = self.client.new_stream("web.runtime.WebRuntime", "WebDocumentRpc", None).await?;
        Ok(Box::new(WebRuntimeWebDocumentRpcStreamImpl { stream }))
    }
    async fn web_worker_rpc(&self) -> starpc::Result<Box<dyn WebRuntimeWebWorkerRpcStream>> {
        let stream = self.client.new_stream("web.runtime.WebRuntime", "WebWorkerRpc", None).await?;
        Ok(Box::new(WebRuntimeWebWorkerRpcStreamImpl { stream }))
    }
}

struct WebRuntimeWatchWebRuntimeStatusStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl WebRuntimeWatchWebRuntimeStatusStream for WebRuntimeWatchWebRuntimeStatusStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn recv(&self) -> starpc::Result<WebRuntimeStatus> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

struct WebRuntimeWebDocumentRpcStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl WebRuntimeWebDocumentRpcStream for WebRuntimeWebDocumentRpcStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<RpcStreamPacket> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

struct WebRuntimeWebWorkerRpcStreamImpl {
    stream: Box<dyn starpc::Stream>,
}

#[starpc::async_trait]
impl WebRuntimeWebWorkerRpcStream for WebRuntimeWebWorkerRpcStreamImpl {
    fn context(&self) -> &starpc::Context {
        self.stream.context()
    }
    async fn send(&self, msg: &RpcStreamPacket) -> starpc::Result<()> {
        self.stream.msg_send(msg).await
    }
    async fn recv(&self) -> starpc::Result<RpcStreamPacket> {
        self.stream.msg_recv().await
    }
    async fn close(&self) -> starpc::Result<()> {
        self.stream.close().await
    }
}

/// Server trait for WebRuntime.
#[starpc::async_trait]
pub trait WebRuntimeServer: Send + Sync {
    /// WatchWebRuntimeStatus.
    async fn watch_web_runtime_status(&self, request: WatchWebRuntimeStatusRequest, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// CreateWebDocument.
    async fn create_web_document(&self, request: CreateWebDocumentRequest) -> starpc::Result<CreateWebDocumentResponse>;
    /// RemoveWebDocument.
    async fn remove_web_document(&self, request: RemoveWebDocumentRequest) -> starpc::Result<RemoveWebDocumentResponse>;
    /// WebDocumentRpc.
    async fn web_document_rpc(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
    /// WebWorkerRpc.
    async fn web_worker_rpc(&self, stream: Box<dyn starpc::Stream>) -> starpc::Result<()>;
}

const WEB_RUNTIME_METHOD_IDS: &[&str] = &[
    "WatchWebRuntimeStatus",
    "CreateWebDocument",
    "RemoveWebDocument",
    "WebDocumentRpc",
    "WebWorkerRpc",
];

/// Handler for WebRuntime.
pub struct WebRuntimeHandler<S: WebRuntimeServer> {
    server: std::sync::Arc<S>,
}

impl<S: WebRuntimeServer + 'static> WebRuntimeHandler<S> {
    /// Creates a new handler wrapping the server implementation.
    pub fn new(server: S) -> Self {
        Self { server: std::sync::Arc::new(server) }
    }

    /// Creates a new handler with a shared server.
    pub fn with_arc(server: std::sync::Arc<S>) -> Self {
        Self { server }
    }
}

#[starpc::async_trait]
impl<S: WebRuntimeServer + 'static> starpc::Invoker for WebRuntimeHandler<S> {
    async fn invoke_method(
        &self,
        _service_id: &str,
        method_id: &str,
        stream: Box<dyn starpc::Stream>,
    ) -> (bool, starpc::Result<()>) {
        match method_id {
            "WatchWebRuntimeStatus" => {
                let request: WatchWebRuntimeStatusRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                (true, self.server.watch_web_runtime_status(request, stream).await)
            }
            "CreateWebDocument" => {
                let request: CreateWebDocumentRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.create_web_document(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "RemoveWebDocument" => {
                let request: RemoveWebDocumentRequest = match stream.msg_recv().await {
                    Ok(r) => r,
                    Err(e) => return (true, Err(e)),
                };
                match self.server.remove_web_document(request).await {
                    Ok(response) => {
                        if let Err(e) = stream.msg_send(&response).await {
                            return (true, Err(e));
                        }
                        (true, Ok(()))
                    }
                    Err(e) => (true, Err(e)),
                }
            }
            "WebDocumentRpc" => {
                (true, self.server.web_document_rpc(stream).await)
            }
            "WebWorkerRpc" => {
                (true, self.server.web_worker_rpc(stream).await)
            }
            _ => (false, Err(starpc::Error::Unimplemented)),
        }
    }
}

impl<S: WebRuntimeServer + 'static> starpc::Handler for WebRuntimeHandler<S> {
    fn service_id(&self) -> &'static str {
        "web.runtime.WebRuntime"
    }

    fn method_ids(&self) -> &'static [&'static str] {
        WEB_RUNTIME_METHOD_IDS
    }
}

