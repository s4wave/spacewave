// @generated by protoc-gen-es-starpc none with parameter "target=ts,ts_nocheck=false"
// @generated from file github.com/aperturerobotics/bldr/web/view/handler/rpc.proto (package web.view.handler, syntax proto3)
/* eslint-disable */

import { HandleWebViewRequest, HandleWebViewResponse } from './rpc.pb.js'
import { MethodKind } from '@aptre/protobuf-es-lite'
import { ProtoRpc } from 'starpc'

/**
 * HandleWebViewService implements the HandleWebView directive.
 *
 * @generated from service web.view.handler.HandleWebViewService
 */
export const HandleWebViewServiceDefinition = {
  typeName: 'web.view.handler.HandleWebViewService',
  methods: {
    /**
     * HandleWebView handles a web view via rpc.
     * The RPC will be held open while the handler runs.
     * The RPC is canceled if the WebView is removed.
     * The handler can access the WebView service via AccessWebViews.
     *
     * @generated from rpc web.view.handler.HandleWebViewService.HandleWebView
     */
    HandleWebView: {
      name: 'HandleWebView',
      I: HandleWebViewRequest,
      O: HandleWebViewResponse,
      kind: MethodKind.Unary,
    },
  },
} as const

/**
 * HandleWebViewService implements the HandleWebView directive.
 *
 * @generated from service web.view.handler.HandleWebViewService
 */
export interface HandleWebViewService {
  /**
   * HandleWebView handles a web view via rpc.
   * The RPC will be held open while the handler runs.
   * The RPC is canceled if the WebView is removed.
   * The handler can access the WebView service via AccessWebViews.
   *
   * @generated from rpc web.view.handler.HandleWebViewService.HandleWebView
   */
  HandleWebView(
    request: HandleWebViewRequest,
    abortSignal?: AbortSignal,
  ): Promise<HandleWebViewResponse>
}

export const HandleWebViewServiceServiceName =
  HandleWebViewServiceDefinition.typeName

export class HandleWebViewServiceClient implements HandleWebViewService {
  private readonly rpc: ProtoRpc
  private readonly service: string
  constructor(rpc: ProtoRpc, opts?: { service?: string }) {
    this.service = opts?.service || HandleWebViewServiceServiceName
    this.rpc = rpc
    this.HandleWebView = this.HandleWebView.bind(this)
  }
  /**
   * HandleWebView handles a web view via rpc.
   * The RPC will be held open while the handler runs.
   * The RPC is canceled if the WebView is removed.
   * The handler can access the WebView service via AccessWebViews.
   *
   * @generated from rpc web.view.handler.HandleWebViewService.HandleWebView
   */
  async HandleWebView(
    request: HandleWebViewRequest,
    abortSignal?: AbortSignal,
  ): Promise<HandleWebViewResponse> {
    const requestMsg = HandleWebViewRequest.create(request)
    const result = await this.rpc.request(
      this.service,
      HandleWebViewServiceDefinition.methods.HandleWebView.name,
      HandleWebViewRequest.toBinary(requestMsg),
      abortSignal || undefined,
    )
    return HandleWebViewResponse.fromBinary(result)
  }
}
